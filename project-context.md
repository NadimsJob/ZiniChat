# Project Context & Developer Log

This file serves as the living memory and source of truth for the **Omnichannel AI Business Assistant Platform**. Every AI agent or developer working on this project must read this file at the start of a task and update it immediately after any architecture changes, feature implementations, or major updates.

---

## 1. Project Overview
A multi-tenant SaaS platform enabling businesses (tenants) to manage customer communication via a unified inbox and self-serve AI Assistants across WhatsApp (Cloud API & Web Baileys), Meta Messenger, Instagram DM, and Web Chat.
* **Primary Architecture Spec**: [full-platform-architecture-v3.md](file:///d:/ZiniChat/full-platform-architecture-v3.md) (v4 Final Production Certified)
* **CRITICAL RULE**: Whenever a new feature is built or an existing feature is modified, the corresponding Help Center manual inside `frontend/src/data/help-docs/` MUST be updated with detailed instructions and screenshots.

---

## 2. Tech Stack & Environment
* **Frontend**: Next.js 16 (App Router) + TypeScript + Tailwind CSS (Unified Dark Glassmorphism Theme `#1F824A` & `#EE8D27`)
* **Backend**: Node.js + NestJS (Modular Monolith)
* **Database**: PostgreSQL with `pgvector` extension (HNSW index optimized) + Prisma ORM
* **Queue/Cache**: Redis + BullMQ
* **Realtime**: Socket.io (namespaces for `/inbox` and `/notifications`)
* **AI Layer**: OpenAI, Anthropic Claude, & Google Gemini SDKs (BYOK & Platform-key mode)
* **Deployment**: Docker Compose + Traefik Reverse Proxy + SSL certificates (Live: `zinichat.com`, Staging: `test.zinichat.com`)

---

## 3. Current Status & Active Focus
- **Current Status**: **100% PRODUCTION READY & CERTIFIED — Complete Code & Security Audit Remediation Applied**
- **Active Focus**: Live deployment via MCP server deployment pipeline (`scripts/invoke-mcp.js`).
- **Core Architecture & Ecosystem Highlights**:
  - **Internal 3 AI Agent Classification (Context Parity)**:
    1. **Inbox Agent** (`OrchestratorService` & `AiService`): Live customer AI across WhatsApp, Messenger, Instagram, Web Chat.
    2. **Simulator Agent** (`AiTrainingService.testSimulate`): AI Training Live Test Simulator inside merchant dashboard.
    3. **ZiniChat Support Agent** (`SupportChatService`): Platform-wide customer support & onboarding AI assistant.
    - *Catalog Parity*: All 3 Agents access tenant-scoped product catalog (standard fields + 100% custom attributes in `Product.attributes`) across all 8 business verticals + retail.
  - **8-Vertical Multi-Industry Adaptation**: Retail + 8 vertical modes (`isPropertyMode`, `isHospitalityMode`, `isTechSoftwareMode`, `isFinancialServiceMode`, `isHealthcareMode`, `isEducationMode`, `isManufacturingMode`, `isLogisticsMode`). Includes specialized team routing (`User.specializationTags`), vertical presets, custom attributes, and conversion analytics. **NOTE: If future changes are made to how products/orders or business types are handled, you MUST check and update the dynamic vertical guidance UI in `SetupWizardModal.tsx` and `AiTrainingReminderPopup.tsx`.**
  - **Security & Audit Remediation**: HMAC signature validation, zero plain-text password leakage, tenant-scoped price/stock verification, AES-256-CBC encryption at rest, DTO validation pipes.

---

## 4. Implementation History Log

| **2026-09-22** | **Help Center Content English Mode Standardization** | `helpDocs.ts`, `help-center/page.tsx`, `project-context.md` | (1) **Help Center Content Display**: Enforced English article titles (`titleEn`), markdown body contents (`contentEn`), excerpts (`excerptEn`), and category titles (`labelEn`) across Help Center (`/dashboard/help-center`) regardless of active UI language selection. (2) **Category Label Fix**: Standardized `marketing-ads` category label `labelBn` to `'Marketing & Ads'`. (3) **100% Passing Verification**: Passed 0-error Next.js TypeScript compilation (`npx tsc --noEmit`). |
| **2026-09-22** | **Feature Rollout Consolidation & Marketing Access Control Fix** | `sp@dmin/ClientLayout.tsx`, `sp@dmin/packages/page.tsx`, `sp@dmin/tenants/page.tsx`, `(tenant)/dashboard/ClientLayout.tsx`, `project-context.md` | (1) **Removed Standalone Menu**: Deleted standalone `/sp@dmin/settings/feature-rollout` menu item & page. (2) **Unified Feature Control**: Consolidated all system feature flags (`meta_ads_agent`, `capi_hub`, `meta_ads_account`, `ads_auto_scaling`) directly into Packages management (`sp@dmin/packages`) and Tenants Customize Plan modal (`sp@dmin/tenants`). (3) **Tenant Sidebar Access Control**: Mapped `/dashboard/ads-agent`, `/dashboard/settings/capi-hub`, and `/dashboard/settings/meta-ads` routes into `featureMap` in `TenantLayout`, properly locking/restricting access unless explicitly allowed by plan or custom feature overrides. (4) **Bengali Label Fix**: Fixed `CAPI Hub` label in Bengali mode to remain `CAPI Hub` in English without translation. (5) **100% Passing Verification**: Passed 0-error TypeScript compilation across Next.js frontend and NestJS backend (`npx tsc --noEmit`). |
| **2026-09-22** | **Automated AI Follow-up Message Engine (BullMQ Delayed Job Architecture)** | `schema.prisma`, `ConversationSidebar.tsx`, `contacts.controller.ts`, `contacts.service.ts`, `contacts.module.ts`, `follow-up.processor.ts`, `follow-up.processor.spec.ts`, `contacts.service.spec.ts`, `helpDocs.ts`, `project-context.md` | (1) **Database Schema**: Added `automatedFollowUpMessage` and `automatedFollowUpSent` to Prisma `Contact` model. (2) **Frontend Inbox UI**: Upgraded `ConversationSidebar.tsx` with date-time picker (`datetime-local`), automated message checkbox, and message textarea with Bengali localization. (3) **BullMQ Delayed Queue Architecture**: Replaced global database polling cron with `FollowUpProcessor` driven by `@nestjs/bullmq` delayed jobs (`follow-up-${contactId}`). Automatically schedules or cancels job upon contact update with zero database polling overhead. (4) **Quota & Attribution Guards**: Enforces AI quota checks and attributes messages to AI Assistant. (5) **Help Center Manual**: Added `automated-ai-followups` guide to `helpDocs.ts` (Rule 8). (6) **100% Test & Build Verification**: Passed all unit tests (`jest src/contacts`, 8/8 tests passing) and 0-error TypeScript compilation across NestJS backend and Next.js frontend (`npx tsc --noEmit`). |
| **2026-09-22** | **External WhatsApp Phone Sync Quota Fix & Dashboard KPI Accuracy** | `inbox.service.ts`, `quota.service.ts`, `tenants.service.ts`, `billing.service.ts`, `tenant-stats.service.ts`, `orchestrator.service.ts`, `payments.service.ts`, `orchestrator.service.spec.ts`, `project-context.md` | (1) **Separated External Sync Messages**: Attached `{ isExternalSync: true }` in `content` when `handleIncomingMessage()` receives outbound messages synced from personal phone WhatsApp apps / WhatsApp Web tabs (`isOutboundFromPhone: true`). (2) **Quota Exclusion Engine**: Updated `QuotaService.getMessageUsage()` and `BillingService.getTenantQuotas()` with Prisma `NOT` filter to exclude external phone app synced messages from deducting from system message quotas (`messagesUsed`). (3) **Dashboard KPI Accuracy**: Updated `TenantStatsService.getDashboardOverview()` to exclude external sync messages from "MESSAGES SENT", "HUMAN REPLIES", and "PLAN QUOTA" KPI cards, preventing quota exhaustion warnings and plan blockages when merchant replies from personal phone app. (4) **100% Passing Verification**: Passed 80/80 backend Jest test suites (618/618 tests passing cleanly) and 0-error TypeScript compilation across backend (`npx tsc --noEmit`). |
| **2026-09-22** | **Phase 9 — Comprehensive Automated Testing Suite & CI/CD Pipeline** | `backend/src/test/mocks/meta-graph-api.mock.ts`, `backend/src/capi-hub/capi-full-pipeline.integration.spec.ts`, `backend/src/ads-agent/ads-copilot-saga.integration.spec.ts`, `backend/src/ads-agent/cross-phase-integration.spec.ts`, `.github/workflows/capi-ads-tests.yml`, `JiniChat-CAPI-MCP-AdsAgent-Implementation-Plan.md`, `project-context.md` | (1) **Meta Graph API Mock Server**: Built `meta-graph-api.mock.ts` intercepting Meta Graph API endpoints for events, insights, campaign/adset/ad creation, image uploads, and policy rejections. (2) **CAPI Pipeline & Webhook Integration Spec**: Built `capi-full-pipeline.integration.spec.ts` testing inbox intent events, duplicate suppression, and external webhooks with secret validation. (3) **Ads Copilot Saga Integration Spec**: Built `ads-copilot-saga.integration.spec.ts` testing end-to-end 4-step saga creation, compensation rollback, unit refunds, and Meta rejection webhooks. (4) **Cross-Phase Integration Spec**: Built `cross-phase-integration.spec.ts` verifying rollout flags, kill-switches, quota guards, and processor crons. (5) **CI/CD Workflow**: Created `.github/workflows/capi-ads-tests.yml`. (6) **100% Passing Verification**: Passed 80/80 backend test suites (618/618 unit and integration tests passing cleanly) and 0-error TypeScript compilation across backend and frontend (`npx tsc --noEmit`). |
| **2026-09-22** | **Phase 8 — Meta App Review Readiness & Help Center Synchronization** | `frontend/src/data/helpDocs.ts`, `backend/docs/META_APP_REVIEW_SUBMISSION_GUIDE.md`, `JiniChat-CAPI-MCP-AdsAgent-Implementation-Plan.md`, `project-context.md` | (1) **Help Center Sync (Rule 8)**: Added `ads-copilot-creator-guide` manual in `helpDocs.ts` covering 5-step AI ad creation flow, Advantage+ audience selection, saga unit refunds, and safety auto-scaling budget cap. Fixed duplicate key error in `helpDocs.ts`. (2) **Meta App Review Submission Guide**: Built comprehensive operational submission guide in `backend/docs/META_APP_REVIEW_SUBMISSION_GUIDE.md` detailing requested permissions (`ads_management`, `ads_read`, `business_management`, `pages_show_list`), test credentials, screencast script, and ClickHouse scalability roadmap. (3) **Build & Test Verification**: Passed 100% Next.js TypeScript compilation (`npx tsc --noEmit`). Marked Phase 8 completed in implementation plan (`[x]`). |
| **2026-09-22** | **Phase 7 — Safety-Guardrail Auto-Scaling Engine & Storage Cleanup Crons** | `ads-agent.service.ts`, `ads-agent.processor.ts`, `ads-agent.controller.ts`, `ads-agent.module.ts`, `ads-agent.processor.spec.ts`, `smtp.service.ts`, `(tenant)/dashboard/ads-agent/page.tsx`, `project-context.md` | (1) **NestJS Auto-Scaling & Purge Scheduler**: Created `AdsAgentProcessor` containing NestJS `@Cron` jobs for 6-hour performance auto-scaling check (`0 */6 * * *`), midnight daily action counter reset (`0 0 * * *`), and 2:00 AM data retention purge (`0 2 * * *`). (2) **Safety Guardrails & Quota Integration**: Implemented `executeCampaignAutoScaling()` enforcing 24h action cap (1/1), 20% budget increase cap, `autoScalingMaxBudget` ceiling, and 5 AI units reservation with instant refund rollback on Meta API scaling failure. Built `purgeOldDataLogs()` deleting 60-day CAPI event logs, 90-day ad campaign drafts, and 6-month AI token usage logs. (3) **Email Notification**: Added `triggerAutoScaleActionEmail()` in `SmtpService`. (4) **Frontend UI**: Upgraded `/dashboard/ads-agent/page.tsx` with Auto-Scaling toggle buttons, Max Budget Cap modal, and active scaling badges. Passed 100% NestJS & Next.js TypeScript compilation (`npx tsc --noEmit`) and 4/4 Jest unit tests. |
| **2026-09-22** | **Phase 6 — Meta Ads Copilot AI Ad Creator, Write Tools & 4-Step Quota Saga Engine** | `schema.prisma`, `mcp-ads.service.ts`, `ads-agent.service.ts`, `ads-agent.controller.ts`, `ads-agent.module.ts`, `ads-agent.service.spec.ts`, `(tenant)/dashboard/ads-agent/page.tsx`, `project-context.md` | (1) **Database Models & Prisma**: Created `AdCampaignDraft` and `AiTokenUsageLog` Prisma models in `schema.prisma` (`npx prisma generate`). (2) **Meta Graph API Write Engine**: Added `createCampaign`, `createAdSet`, `createAd`, `uploadAdImage`, `pauseCampaign`, `resumeCampaign`, and `pauseAdSet` write methods to `McpAdsService`. (3) **5-Turn Conversation & 4-Step Quota Saga Engine**: Built `AdsAgentService` handling 5-turn AI guided campaign creation flow (Turn 1: Catalog RAG product selector, Turn 2: Advantage+ Audience & Location, Turn 3: Budget & Placements with balance check telemetry, Turn 4: Creative Review & Image Hash resolution, Turn 5: Final Approval Card with 10 AI Response units reservation notice). Implemented 4-step Saga transaction with automatic compensation rollback and unit refunds on step failure or Meta policy rejection. (4) **Tenant Panel UI**: Upgraded `/dashboard/ads-agent/page.tsx` with interactive 5-turn AI chat creator tab, live ad mockup previewer, and campaign manager table with pause/resume controls. Passed 100% NestJS & Next.js TypeScript compilation (`npx tsc --noEmit`) and 9/9 Jest unit tests. |
| **2026-09-21** | **Phase 5 — Meta Ads Copilot MCP Server & Read-Only Tools Engine** | `mcp-ads.service.ts`, `mcp-ads.server.ts`, `mcp-ads.controller.ts`, `mcp-ads.module.ts`, `(tenant)/dashboard/ads-agent/page.tsx`, `ClientLayout.tsx`, `project-context.md` | (1) **Backend MCP Engine**: Installed `@modelcontextprotocol/sdk`. Built `McpAdsService`, `McpAdsServer`, and `@Controller('mcp-ads')` exposing REST & MCP tools (`get_ad_insights`, `list_campaigns`, `list_adsets`, `get_ad_account_balance`) with tenant isolation and token decryption. (2) **Tenant Panel UI**: Created `<AdsAgentPage />` at `/dashboard/ads-agent` featuring bilingual guidance, `useRolloutFlag('meta_ads_agent')` gate, AI quota sharing notice banner, ad account balance telemetry, campaigns list, and registered read-only MCP tools display. (3) **Sidebar Integration**: Linked `Ads Copilot` under `MARKETING & ADS` category in `ClientLayout.tsx`. Passed 100% backend & frontend TypeScript compilation (`npx tsc --noEmit`) and 6/6 Jest unit tests. |
| **2026-09-21** | **Fix Live Inbox Real-Time Message Preview Snippet & 'No messages' Fallback Bug** | `(tenant)/dashboard/inbox/page.tsx`, `project-context.md` | Fixed Live Inbox conversation list snippet displaying "No messages" when incoming messages arrive via real-time WebSocket (`new_message`). Root cause: `socket.on('new_message')` in `inbox/page.tsx` updated `lastMessageAt` on conversation state but left `messages` array unchanged/empty, causing `conv.messages?.[0]` to evaluate as undefined and fall back to "No messages". Solution: Updated socket handler to inject `data.message` at index 0 of `updatedConv.messages` and `newConv.messages`, ensuring the message preview snippet displays real-time message text instantly for both incoming customer messages and outgoing agent/AI replies. Verified 0-error TypeScript compilation (`npx tsc --noEmit`). |
| **2026-09-20** | **Enforce Meta Cloud API & Connectivity Check for Broadcasts** | `broadcasts.service.ts`, `broadcasts.controller.ts`, `(tenant)/dashboard/broadcasts/page.tsx`, `project-context.md` | (1) **Enforce CLOUD_API**: Updated `BroadcastsService` to explicitly query `provider: 'CLOUD_API'` to prevent `WEB_QR` channels from creating templates or broadcasting. (2) **Meta Connectivity Check Endpoint**: Created `getMetaConnectionStatus()` and `@Get('meta-status')` to actively ping Meta Graph API to verify WABA token health. (3) **Frontend Warning UI**: Upgraded `BroadcastsPage` to fetch Meta status on load, displaying a red warning banner and disabling actions if the Meta Cloud API connection is invalid or missing. Verified backend & frontend TS builds (`npx tsc --noEmit`). |
| **2026-09-20** | **Team Member Live Presence Badges & Clickable Activity/Stats Modal** | `tenant-team.service.ts`, `tenant-team.controller.ts`, `(tenant)/dashboard/team/page.tsx`, `project-context.md` | (1) **Live Presence Badges**: Embedded real-time presence indicators (`available` 🟢, `busy` 🟡, `away` 🟠, `offline` ⚪) next to team avatars in `/dashboard/settings/team`. (2) **Member Activity & Access Stats Endpoint**: Added `@Get(':id/stats')` in `tenant-team.controller.ts` returning handled conversations, assigned leads count, and recent 5 login sessions with IP/Browser metadata. (3) **Clickable Stats Modal**: Built `<ActivityStatsModal />` in frontend with bilingual support. Passed 0-error NestJS and Next.js TypeScript compilation checks. |
| **2026-09-20** | **Tenant Sidebar Menu Category Re-organization (Team Management under SETTINGS)** | `ClientLayout.tsx`, `project-context.md` | Re-organized tenant dashboard navigation bar in `ClientLayout.tsx` by moving **Team Management** (`/dashboard/settings/team`, icon: `Users`) from `OTHERS` into the **SETTINGS** category. Updated `featureMap` to include `/dashboard/settings/team`. Verified 0 TypeScript compilation errors (`npx tsc --noEmit`). |
| **2026-09-20** | **Help Center Documentation & Feature Screenshots 100% English Standardization** | `helpDocs.ts`, `frontend/public/docs/*`, `project-context.md` | (1) **100% English Articles**: Updated all 14 articles in `frontend/src/data/helpDocs.ts` to pure standard English across all titles, excerpts, content, tags, and category labels. (2) **100% English UI Screenshots**: Converted all 16 vector SVG & PNG asset mockups in `frontend/public/docs/` (`connect-channel`, `ai-training-setup`, `inbox-messaging-guide`, `sales-orders-guide`, `billing-mfs-guide`, `settings-team-guide`, `website-widget-guide`, `leads-kanban-guide`) to 100% English text. (3) **Deployment**: Committed (`805089a`), pushed to `main`, and deployed live via MCP pipeline (`invoke-mcp.js`). |
| **2026-09-20** | **New User Setup Wizard Auto-Popup & 3-Min AI Training Reminder Integration** | `auth.service.ts`, `SetupWizardModal.tsx`, `AiTrainingReminderPopup.tsx`, `(tenant)/dashboard/page.tsx`, `project-context.md` | (1) **Backend Updates**: Extended `getSetupStatus` in `auth.service.ts` to return `aiTrainingScore` (0-100), `qnaCount`, and `businessNature`. (2) **Guided Setup Wizard Modal**: Built `<SetupWizardModal />` that auto-opens for new users with incomplete setup, featuring a 3-step flow (Connect Channel → Train AI/Persona → Business-Vertical specific product addition guidance). (3) **AI Training 3-Min Reminder Popup**: Created `<AiTrainingReminderPopup />` which persistently reappears every 3 minutes for users with `aiTrainingScore < 50`, prompting them to fully complete Q&As and Product additions, only stopping if they click "I'm fully set up" or achieve a score ≥ 50. (4) **Integration**: Embedded both components in `/dashboard/page.tsx` and linked them to `setupStatus` fetch responses with robust `localStorage` dismissal flags. |
| **2026-09-20** | **Mandatory Field Red Star (*) Indicator Integration for General Signup & Google Onboarding Forms** | `(auth)/signup/page.tsx`, `(tenant)/dashboard/onboarding/page.tsx`, `project-context.md` | (1) **Google / Profile Onboarding Form**: Added red star (`*`) mandatory indicators (`<Req />`) to all required input labels (Owner Name, Brand Name, Country, Phone Number, No. of Employees, Business Nature, Business Address, Set Account Password) and updated label contrast for dark mode (`dark:text-zinc-200`). (2) **General Signup Form**: Ensured all mandatory fields (Business Name, Country, Phone No., Full Name, Email Address, Employee Count, Business Nature, Password, Confirm Password) have consistent red star (`*`) indicators. Verified 0 TypeScript compilation errors. |
| **2026-09-18** | **Product Photo Matching & Image Reading AI Credit Audit & Multi-Credit Logging Fix** | `orchestrator.service.ts`, `ai.service.ts`, `(tenant)/dashboard/settings/ai-training/page.tsx`, `project-context.md` | (1) **Image Reading Credit Fix**: Fixed `recordUsageLog()` in `ai.service.ts` & `orchestrator.service.ts` so when Vision/Image Reading runs (`canRunVision = true`, `creditsNeeded = 5`), it logs 5 credit entries into `aiUsageLog` regardless of whether LLM token usage metadata is returned, ensuring exact 5 credits deduction in tenant quotas. (2) **Product Photo Matching Credit Clarification**: Verified that Product Photo Matcher does not run expensive multi-image vision API calls; Gemini Vision reads the customer image 1 time (5 credits), and the backend instantly matches catalog products via deterministic DB token search (0 additional credits). Total cost per product image match is 5 credits. (3) **UI Label Documentation**: Updated tool toggles description in AI Training settings page to state `(Uses Image Reading: 5 AI Responses per image)` in EN & BN. Passed 0-error NestJS and Next.js builds & Jest unit tests. |
| **2026-09-16** | **Restore Tenant Support AI Agent (`SupportWidget`) & Strict External Website Widget Isolation from Dashboard** | `ClientLayout.tsx`, `widget.js`, `wa-widget.js`, `project-context.md` | (1) **Restored Tenant Support Agent**: Restored `<SupportWidget />` in `ClientLayout.tsx` so tenant merchants retain full access to their platform ZiniChat Support AI agent. (2) **External Website Widget Isolation**: Fixed purple marketing website live chat widget (`widget.js` `#zc-livechat-container`) leaking into tenant panel during client-side SPA navigation from marketing pages. Added `useEffect` in `ClientLayout.tsx` that instantly purges `#zc-livechat-container` and `#zc-wa-float-container` from `document.body` on `/dashboard/*` routes, and added `window.location.pathname` guards in `widget.js` & `wa-widget.js`. Verified 0-error TypeScript build. |
| **2026-09-16** | **Mobile Bottom Navigation Bar Layout Fix, Uniform Button Sizing & High-Contrast Light/Dark Styling** | `ClientLayout.tsx`, `(tenant)/dashboard/leads/page.tsx`, `project-context.md` | (1) **Mobile Bottom Nav Fix**: Fixed broken/unconstrained `Orders` link in `ClientLayout.tsx` by adding missing flex container classes (`flex flex-col items-center justify-center w-full h-full text-[11px] font-semibold`), standardizing icon sizes to `w-5 h-5 mb-0.5`, and applying vertical mode dynamic label resolution (`Orders`, `Inquiries`, `Reservations`, `Demos`, `Consultations`, `Appointments`, `Admissions`, `RFQ`, `Shipments`). (2) **High-Contrast Bright Colors**: Replaced dull `text-muted-foreground` with bright, high-contrast text color pairings (`text-slate-700 dark:text-zinc-200 hover:text-primary` inactive, `text-primary font-bold scale-105` active) for crisp visibility in both Light Mode and Dark Mode across all responsive devices. (3) **Leads Page Header Buttons**: Added `whitespace-nowrap shrink-0` to `+ Add Lead` button preventing vertical 2-line text wrap, and upgraded `Export` & view switcher buttons with bright eye-soothing themes. Verified 0-error TypeScript build. |
| **2026-09-15** | **Order Requests Auto-Population, Manual-Only AI Summary Controls & Header Channel "More" Dropdown Redesign** | `orchestrator.service.ts`, `inbox.service.ts`, `(tenant)/dashboard/inbox/page.tsx`, `project-context.md` | (1) **Order Requests Auto-Population**: Updated all 8 vertical AI intent handlers (`handleDemoRequest`, `handleConsultationRequest`, `handleAppointmentRequest`, `handleRoomBookingInquiry`, `handleBulkRfqInquiry`, `handleShipmentInquiry`, `handlePropertyInquiry`, `handleCourseAdmissionInquiry`) in `orchestrator.service.ts` to set `hasOrderRequest: true` on `Conversation`. Added `onModuleInit()` retroactive sync in `InboxService` to populate `hasOrderRequest = true` for existing demo/booking conversations (e.g. Major Nadim). (2) **Manual-Only AI Summary & Credit Safeguard**: Completely removed background `checkAndTriggerSummarization()` trigger from `InboxService` so summaries are NEVER automatically generated after 12 messages. AI summaries are now strictly generated only when the merchant clicks "Generate AI Summary" or "Regenerate", deducting 1 AI credit via `QuotaService` validation and `AiUsageLog`. (3) **Header UI Redesign (Visual Separation & More Dropdown)**: Added a vertical line divider (`h-6 w-[2px] bg-border/80`) separating Smart Filters (Left) from Channel Filters (Right). Applied distinct theme color palettes (Indigo for Conversation Filters, Emerald for Channel Filters). Added a "More (আরো)" dropdown popover menu for overflow channels to eliminate horizontal scroll friction. Passed 0-error NestJS and Next.js TypeScript builds. |
| **2026-09-15** | **Fix AI Conversation Flow Loss on Short Customer Replies (Context-Aware RAG Augmentation & Mandatory Continuity Rule)** | `orchestrator.service.ts`, `ai-training.service.ts`, `project-context.md` | Fixed AI losing conversation flow when customer sends short affirmations/negations like "হ্যাঁ", "না", "জানতে চাই", "বলুন", "yes", "ok" after the AI asked a question. Root cause: Stage 0 RAG retrieval in `OrchestratorService.processMessage()` used only the current short `userText` (e.g. "হ্যাঁ") as the vector search query — these short words don't match any products/Q&As, so the AI received no product context and lost the topic. Fix: (1) Context-aware RAG augmentation — for messages ≤5 words, fetches the last AI-sent outbound message from DB and prepends it to the RAG query (e.g. "Tell me about Product X + হ্যাঁ"), ensuring the right product context is retrieved. (2) Added mandatory `=== CRITICAL CONVERSATION CONTINUITY RULE ===` instruction block in `buildContextPrompt()` explicitly telling the AI to read conversation history to understand what the customer is responding to, and continue the same topic/flow. (3) Mirrored both fixes in `AiTrainingService.testSimulate()` for Simulator Agent parity. All 3 AI agents (Inbox, Simulator, Support) now maintain conversation flow through short customer replies. Passed 0-error TypeScript build. |
| **2026-09-15** | **Inbox Display Name Rename Capability (Backend Endpoint & Frontend Inline Editing)** | `inbox.service.ts`, `inbox.controller.ts`, `inbox.controller.spec.ts`, `(tenant)/dashboard/settings/inboxes/page.tsx`, `project-context.md` | (1) Added `renameChannel(tenantId, id, name)` in `InboxService` & `@Patch('channels/:id/rename')` endpoint in `InboxController` allowing merchants to rename any connected channel (`ChannelConnection.displayName`) or website widget (`WebsiteWidget.name`). (2) Added Pencil edit icon and inline editing input in `/dashboard/settings/inboxes/page.tsx` with instant keyboard shortcuts (Enter to save, Escape to cancel) and optimistic UI state update. Passed 0-error NestJS build & Jest unit tests. |
| **2026-09-15** | **Live Inbox Hard-Delete Conversation Confirmation Modal, Disk Media Purge & Storage Reclamation** | `inbox.service.ts`, `(tenant)/dashboard/inbox/page.tsx`, `ConversationSidebar.tsx`, `project-context.md` | (1) Upgraded `deleteConversation()` in `inbox.service.ts` to scan all conversation messages for media file URLs (`/uploads/tenants/...`), physically `fs.unlinkSync()` images, videos, documents & voice notes from server disk storage, and decrement `tenant.storageUsedBytes` so physical disk space & storage quota are fully reclaimed when a conversation is deleted. (2) Added a high-contrast Hard Delete Confirmation Modal when clicking the Trash icon in Live Inbox (`/dashboard/inbox`), prompting user before executing hard-delete. (3) Removed `ID: {externalContactId}` line under phone number in `ConversationSidebar.tsx`. Passed 0-error TypeScript build. |
| **2026-09-15** | **Live Inbox Header Channel Filter UI/UX Redesign (`whitespace-nowrap`, `no-scrollbar` & Pill Styles)** | `(tenant)/dashboard/inbox/page.tsx`, `project-context.md` | Fixed Live Inbox top channel filter tab text wrapping into 2 vertical lines (e.g., "All \n channels", "FB \n Comments") and ugly OS scrollbars overlapping tabs. Root cause: Missing `whitespace-nowrap` & `shrink-0` on button labels, restrictive `max-w-[500px]` container width, and `custom-scrollbar` track. Solution: (1) Added `whitespace-nowrap shrink-0 inline-flex items-center gap-1.5` to all tab buttons preventing 2-line linebreaks, (2) Replaced `max-w-[500px]` with fluid flex container using `no-scrollbar` utility (hides thick scrollbars in light/dark modes while supporting touch/wheel horizontal scrolling), (3) Upgraded pill styling with eye-soothing theme colors (Emerald for All Channels, Orange for FB Comments, Primary soft tint for active channels). Passed 0-error TypeScript build. |
| **2026-09-15** | **Fix Website Widget Quota & Automatic Hard-Delete Purge** | `website-widget.service.ts`, `inbox.service.ts`, `website-widget.service.spec.ts`, `project-context.md` | Fixed website widget creation error `Website widget limit reached (3/2)` for tenants with remaining active widget quota (e.g. `job.nadim30@gmail.com`). Root cause: `getWidgetQuota()` in `website-widget.service.ts` counted all records in `website_widgets` table without filtering by `isActive: true`. Solution: (1) Added `where: { tenantId, isActive: true }` filter in `getWidgetQuota()` and `getWidgets()`, (2) Added `OnModuleInit` lifecycle hook in `WebsiteWidgetService` that automatically executes `prisma.websiteWidget.deleteMany({ where: { isActive: false } })` on boot to hard-delete all inactive widgets from PostgreSQL so DB never gets bloated, (3) Updated `inbox.service.ts` `toggleChannelActiveStatus` to hard-delete website widgets immediately upon deactivation. Passed 16/16 unit tests and 0-error TypeScript build. |
| **2026-09-15** | **Fix Live Chat Lead Capture Contact Name Overwrite & Real-Time Socket Sync** | `website-widget.service.ts`, `project-context.md` | Fixed website chat visitors' submitted lead form names (e.g., "Nadim") being overwritten back to `Website Visitor (ke2y)` in live inbox. Root cause: `sendVisitorMessage()` in `website-widget.service.ts` had a hardcoded string `contactName: Website Visitor (${visitorId.slice(-4)})` when passing messages to `InboxService.handleIncomingMessage()`, which reverted DB contact name updates. Solution: (1) Resolved dynamic contact name from `leadInfo.name` -> `existingContact.name` -> `Website Visitor (${id})`, (2) Passed resolved contact name to `handleIncomingMessage()`, (3) Dispatched real-time `contact:updated` WebSocket broadcast via `InboxGateway` on lead form submission so live inbox instantly updates contact name, phone, and email across all merchant screens without manual refresh. |
| **2026-09-15** | **Fix Desktop Bottom-Right Widget Alignment (Font-Family Quote Typo Fix & Safe DOM Mount)** | `widget.js`, `wa-widget.js`, `project-context.md` | Fixed widget button jumping to bottom-left corner on desktop web screens. Root cause: An unclosed double-quote inside `font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI` in `widget.js` inline `cssText` corrupted the CSS property string, causing browsers to invalidate `right: 20px;` and default to `left: 0`. Solution: Cleaned `font-family: system-ui, -apple-system, sans-serif` string, added explicit desktop rule `#zc-livechat-container { position: fixed !important; right: 20px !important; bottom: 20px !important; left: auto !important; }`, and added `safeAppend()` DOM-ready guard guaranteeing widget rendering on 100% of devices. Passed 0-error TypeScript checks. |
| **2026-09-15** | **Fix Mobile Horizontal Viewport Scroll & Page Zoom Out (`overflow-x: hidden` & Floating Stacking Fix)** | `globals.css`, `widget.js`, `wa-widget.js`, `project-context.md` | Fixed website creating a horizontal scroll section on the right on mobile load, which caused the page to shrink/zoom out and pushed the widget button into the right-hand scroll overflow area. Root cause: Missing document-level viewport overflow guard and unconstrained box-sizing in floating widgets. Solution: (1) Added `html, body { max-width: 100vw; overflow-x: hidden; }` in `globals.css` preventing horizontal viewport expansion, (2) Added `#zc-livechat-container, #zc-livechat-container * { box-sizing: border-box !important; }` and `#zc-wa-float-container, #zc-wa-float-container * { box-sizing: border-box !important; }` in `widget.js` and `wa-widget.js`, (3) Enforced fixed bottom-right floating overlay (`position: fixed !important; right: 16px !important; bottom: 16px !important`). The floating widget button now floats naturally on top of the mobile page without expanding page width or triggering zoom out. Passed 0-error TypeScript checks. |
| **2026-09-15** | **Live Chat Pre-Chat Lead Capture Form Field Labels & High-Contrast Inputs** | `widget.js`, `project-context.md` | Enhanced the Live Chat Pre-Chat Lead Capture Form in `widget.js` SDK. Added explicit, high-contrast bilingual `<label>` elements (**Full Name / আপনার নাম ***, **Phone Number / ফোন নম্বর ***, **Email Address / ইমেইল ঠিকানা**) above each input field, styled inputs with crisp dark text (`color: #0f172a`, `border: 1px solid #cbd5e1`), added clear bilingual placeholder examples, and added `input::placeholder` rule (`color: #94a3b8 !important`). Passed 0-error TypeScript checks (`npx tsc --noEmit`). |
| **2026-09-15** | **Fix Mobile Widget Disappearing / Off-Screen Shift (`margin-left: auto` & Stacking Layout Fix)** | `widget.js`, `wa-widget.js`, `project-context.md` | Fixed floating chat widget button appearing briefly on mobile page load and then disappearing off-screen. Root cause: Mobile Safari / WebKit layout engine computed `margin-left: auto !important` on fixed container with `max-width: calc(100vw - 32px)` as a huge ~360px left margin, shifting `#zc-livechat-btn` completely off the right edge of mobile viewports. Solution: Converted `#zc-livechat-container` and `#zc-wa-float-container` to vertical flex containers (`display: flex !important; flex-direction: column !important; align-items: flex-end !important`), removed problematic `margin-left: auto`, added `pointer-events: none` on container with `pointer-events: auto` on interactive children, and added iOS safe area inset support (`bottom: calc(16px + env(safe-area-inset-bottom, 0px))`). Verified 0 TypeScript errors. |
| **2026-09-15** | **Fix Landing Page Hero Section Mockup Mobile Horizontal Overflow** | `HomePageContent.tsx`, `SetupWidgetMockup.tsx`, `project-context.md` | Fixed hero section mockup cards (`WhatsAppBotMockup` & `SetupWidgetMockup`) and floating badge icons (`MessengerBadgeIcon`, `InstagramBadgeIcon`) spilling horizontally on mobile devices (e.g., iPhone 15 Pro Max). Adjusted `WhatsAppBotMockup` container to `max-w-[320px] sm:max-w-sm`, moved floating platform badges to `right-1`/`left-1` on mobile, and disabled 3D tilt transformation on mobile screens (`sm:rotate-y-[-10deg] sm:rotate-x-[5deg]`). Verified 0 TypeScript errors. |
| **2026-09-15** | **Fix Mobile Widget Overlap & Off-Screen Overflow (`right: 16px !important`)** | `widget.js`, `wa-widget.js`, `(marketing)/layout.tsx`, `project-context.md` | Fixed floating chat widget button being cut off on mobile screens (e.g. iPhone 15 Pro Max). Root cause: Marketing layout had duplicate script tags (`wa-widget.js` + `widget.js`) rendering two floating buttons at the same position, causing button collision and right-edge overflow. Removed duplicate `wa-widget.js` script tag from `MarketingLayout` and injected explicit mobile CSS (`max-width: calc(100vw - 32px) !important; right: 16px !important; margin-left: auto !important`) into `widget.js` and `wa-widget.js`. Passed 0-error TypeScript checks. |
| **2026-09-15** | **Live Chat Widget Phase 2 — HTTP Message Polling, Mobile Responsiveness, Session Tracking & Pre-Chat Lead Form** | `schema.prisma`, `website-widget.service.ts`, `website-widget.controller.ts`, `widget.js`, `inboxes/page.tsx`, `project-context.md` | Added `requireLeadCapture` and `leadCaptureFields` to `WebsiteWidget` schema (`npx prisma db push` + `npx prisma generate`). Added public `GET /website-widget/public/messages?token=...&visitorId=...&after=...` endpoint for long-polling incoming agent/AI responses. Upgraded `widget.js` SDK with: (1) 3s HTTP message polling for real-time AI reply rendering, (2) Full-screen mobile modal layout (`100vw`, `100dvh`) for screens `<= 640px` width, (3) 30-minute activity session tracking (`localStorage` `__zc_sess_*`) with manual new chat reset button, (4) Pre-Chat Lead Capture Form (Name, Phone, Email) with pre-chat submit validation and initial lead context transfer to AI. Updated `WidgetConfigModal` on inboxes page with Pre-Chat Lead Form toggle, field checkboxes, and live preview. Passed all TypeScript checks (`npx tsc --noEmit`) with 0 errors. |
| **2026-09-15** | **Fix Real-time Inbox Broadcast, AI Auto-Reply Guard & Mobile Widget Responsiveness** | `inbox.service.ts`, `orchestrator.service.ts`, `widget.js`, `project-context.md` | Added real-time Socket.io `new_message` event emission inside `InboxService.handleIncomingMessage()` so all incoming messages (including website chat) immediately update merchant dashboard inboxes without refreshing. Fixed `OrchestratorService` website channel active check so AI auto-replies trigger seamlessly. Injected media-query CSS in `widget.js` for screen widths `<= 640px` so live chat drawer windows render 100% responsively on mobile devices. Passed 0-error TypeScript checks. |
| **2026-09-15** | **Wire Live Chat Widget SDK to Backend Inbox & Restrict Widget to Marketing Pages** | `website-widget.service.ts`, `website-widget.controller.ts`, `website-widget.module.ts`, `widget.js`, `layout.tsx`, `(marketing)/layout.tsx`, `project-context.md` | Created public POST endpoint `@Post('public/message')` in `website-widget.controller.ts` that looks up the widget by token and calls `InboxService.handleIncomingMessage()` to deliver visitor messages to the tenant inbox and trigger AI auto-replies. Updated `widget.js` SDK with real HTTP POST calls and `localStorage` `visitorId` persistence (`__zc_vid`). Moved widget embed script tags from global `RootLayout` (`frontend/src/app/layout.tsx`) to `MarketingLayout` (`frontend/src/app/(marketing)/layout.tsx`), completely preventing the chat widget from rendering inside tenant dashboard (`/dashboard/*`) and superadmin panels. |
| **2026-09-15** | **Build Live Chat Public Embed Script SDK (`widget.js`)** | `widget.js`, `layout.tsx`, `project-context.md` | Created missing `frontend/public/widget.js` SDK file. It parses `data-token`, `data-color`, `data-heading`, and `data-position`, fetches public widget configuration from `GET /website-widget/public/:token`, and mounts an interactive Live Chat floating trigger button and chat drawer window on external sites. |
| **2026-09-15** | **Embed Test Tenant Live Chat Widget Script into RootLayout** | `layout.tsx`, `project-context.md` | Embedded tenant Live Chat widget script tag (`<Script src="https://zinichat.com/widget.js" data-token="622de982-7993-4778-b7c6-bf16c042dcb4" data-color="#7C3AED" data-heading="Chat with us" />`) into global `RootLayout` (`frontend/src/app/layout.tsx`) for real-world embed validation. |
| **2026-09-15** | **Strict Single-Widget Type Locking in Edit Modal (`WidgetConfigModal`)** | `inboxes/page.tsx`, `project-context.md` | Removed the Widget Type switcher toggle (`উইজেটের ধরণ`) from `WidgetConfigModal`. Opening a Live Chat widget now displays exclusively Live Chat settings, headers, live chat preview, and `widget.js` embed code; opening a WhatsApp widget displays exclusively WhatsApp settings, phone URL, button preview, and `wa-widget.js` embed code. Users can no longer inadvertently switch widget types during edit mode. |
| **2026-09-15** | **Website Widget Embed Code & Installation Guide Standardization** | `inboxes/page.tsx`, `inboxes/new/page.tsx`, `project-context.md` | Unified embed code generator (`getWidgetEmbedCode` & `getEmbedCode`) across Edit Modal and Setup Wizard Step 4. Both WhatsApp (`wa-widget.js`) and Live Chat (`widget.js`) widgets now display clean multi-line script tags with all configured data attributes (`data-token`, `data-phone`, `data-color`, `data-position`, `data-heading`). Added a 4-step bilingual installation guide (EN/BN) inside `WidgetConfigModal`. |
| **2026-09-15** | **Fix Inboxes Page Character Encoding Mojibake & Status Badge Emoji Rendering** | `inboxes/page.tsx`, `project-context.md` | Restored clean UTF-8 encoding for Bangla strings across Connected Inboxes page (`/dashboard/settings/inboxes`), eliminating Mojibake corruption (`à¦...`). Removed redundant raw emojis (`🟢`, `🔴`, `⚪`) next to status text to prevent character encoding glitches (`Active ðŸŸC`), relying on the animated CSS status dot. Passed 0-error TypeScript check (`npx tsc --noEmit`). |
| **2026-09-14** | **Global Platform WhatsApp Widget Integration (`wa-widget.js`)** | `layout.tsx`, `project-context.md` | Embedded WhatsApp Widget script tag (`<Script src="https://zinichat.com/wa-widget.js" data-token="ff0c5def-9c1d-410b-9a5b-a15cb8f90a0c" strategy="afterInteractive" />`) into global `RootLayout` (`frontend/src/app/layout.tsx`). The WhatsApp button now renders seamlessly across all pages of `zinichat.com`. |
| **2026-09-14** | **1st-Time WhatsApp Widget Setup & Real-time Visual Preview Engine** | `inboxes/new/page.tsx`, `project-context.md` | Upgraded 1st-time WhatsApp website widget creation flow (`/dashboard/settings/inboxes/new`) with full 2-column layout. Added complete customization controls (target WhatsApp channel picker, widget name, theme color selector, custom icon upload, prefilled text message, screen position, and Bangla tooltip text) along with live interactive visual preview showing real-time floating WhatsApp button, ping animation, custom icon / vector SVG icon, target `https://wa.me/...` URL, and animated tooltip text. Verified 0-error TypeScript build. |
| **2026-09-14** | **Website Widget Permanent Hard Delete Engine** | `inbox.service.ts`, `website-widget.service.ts`, `website-widget.service.spec.ts`, `inboxes/page.tsx`, `project-context.md` | Replaced soft-delete (`isActive: false`) with permanent Prisma hard delete (`prisma.websiteWidget.delete()`) across `inbox.service.ts` and `website-widget.service.ts`. Added `isActive: true` filter to `getChannels` query in `inbox.service.ts` so deleted website widgets are completely wiped from DB and never linger as inactive in the inbox list. Updated unit test suite (16/16 passed) and fixed frontend preview JSX syntax errors. |
| **2026-09-14** | **Strict Separation of Website Live Chat Widget & WhatsApp Widget** | `inboxes/page.tsx`, `inboxes/new/page.tsx`, `project-context.md` | Separated `LIVE_CHAT` Website Widget and `WHATSAPP` Website Widget into 2 completely independent, unmixed widget types. Removed the `WIDGET TYPE` switcher toggle from `WidgetConfigModal` so opening a Live Chat widget displays Live Chat controls, `widget.js` embed code, and live chat preview, while opening a WhatsApp widget displays WhatsApp controls, `wa-widget.js` embed code, and floating button preview. Upgraded Live Chat Widget creation form (`/dashboard/settings/inboxes/new`) with real-time visual preview. |
| **2026-09-14** | **WhatsApp Widget Connected Channel Auto-Sync (Read-Only)** | `inboxes/page.tsx`, `project-context.md` | Removed manual text input field for WhatsApp phone number in Website Widget config modal (`/dashboard/settings/inboxes`). Automatically populates tenant's connected WhatsApp channel phone number (`+8801...`), sets lock/read-only badge `🔒 Auto-Synced (Read-Only)`, dynamically generates target redirection URL (`https://wa.me/<number>?text=...`), and supports connected channel selection if multiple WhatsApp channels exist. |
| **2026-09-14** | **Tenant WhatsApp Website Widget & Public Embed Script SDK (`wa-widget.js`)** | `inboxes/page.tsx`, `wa-widget.js`, `website-widget.controller.ts`, `website-widget.service.ts`, `schema.prisma` | Cancelled/reverted Superadmin global widget. Built Tenant Panel WhatsApp Widget feature under `/dashboard/settings/inboxes`. Tenants can select/configure WhatsApp phone number (`https://wa.me/<number>?text=...`), choose theme button color, toggle between vector WhatsApp SVG icon and custom uploaded icon image (`/website-widget/upload-icon`), set screen position, and generate 1-click embed code (`<script src="https://zinichat.com/wa-widget.js" data-token="..."></script>`) for their external websites with live interactive preview. |
| **2026-09-14** | **Bangla QR Image 404 Fix — `getQrImageUrl()` & Next.js Rewrite** | `pay-mfs/page.tsx`, `next.config.ts` | Root cause: `getQrImageUrl()` was converting `/uploads/mfs/...` to `/api/uploads/mfs/...` but `next.config.ts` had no rewrite rule to proxy `/api/uploads/*` to backend. Fix: (1) Added `async rewrites()` in `next.config.ts` to proxy `/api/uploads/:path*` → `NEXT_PUBLIC_API_URL/uploads/:path*`. (2) Fixed `getQrImageUrl()` to use `${API}/uploads/...` absolute URL directly from backend domain, so QR image loads from `api.zinichat.com/uploads/mfs/...` correctly. File is confirmed present on server with `rw-r--r--` permissions and HTTP 200 from backend. |
| **2026-09-14** | **Feature Locked Dark Mode Glassmorphism Overhaul & MFS QR Fallback Engine** | `ClientLayout.tsx`, `pay-mfs/page.tsx` | Overhauled Feature Locked modal with dark mode glassmorphism backdrop (`dark:bg-zinc-900`, `dark:border-amber-500/20`), high-contrast buttons, and scrollable container. Added client `onError` automatic fallback on payment QR image to render dynamic EMVCo Bangla QR payload, and verified mobile native payment verification flow. |
| **2026-09-14** | **Static Uploads Path Resolution & File Permissions Fix** | `main.ts`, `mfs-payments.controller.ts`, `products.controller.ts` | Fixed NestJS static assets directory in production (`main.ts`) to serve directly from `process.cwd()/uploads` instead of `dist/uploads`, preventing 404 image errors on live server for uploaded MFS QR codes (`/uploads/mfs/...`). Added automatic `chmod 0o644` file permissions on upload. |
| **2026-09-14** | **Feature Locked Modal Close Contrast & Superadmin Tenants Phone / Business Nature Columns** | `ClientLayout.tsx`, `tenants.service.ts`, `sp@dmin/tenants/page.tsx` | Fixed white-on-white unreadable Close button text in Feature Locked modal with high contrast light/dark mode styles, and added `Phone` and `Business Nature` columns to Superadmin Tenants list table (`/sp@dmin/tenants`). |
| **2026-09-14** | **Meta Integration Section Portfolio ID Removal & Layout Simplification** | `MetaIntegrationSection.tsx` | Removed Portfolio ID text from hero description and removed the 3 lower platform cards to streamline the showcase section across Home and Success Stories pages. |
| **2026-09-14** | **Official Meta Tech Provider FAQ Questions & Answers** | `landing-page.service.ts` | Added dedicated Meta Official Tech Provider questions & answers to landing page FAQ JSON (`OFFICIAL_FAQS_JSON`), updated existing channel & security FAQs with Graph API partner details, and configured automatic sync for live landing page config. |
| **2026-09-14** | **Superadmin Tenant Mobile Display, Profile Edit & Direct Password Reset** | `tenants.service.ts`, `tenants.controller.ts`, `tenants.service.spec.ts`, `sp@dmin/tenants/[id]/page.tsx` | Added `phoneNo` display row to Business Profile card in Superadmin Tenant View, built Edit Profile modal (businessName, brandName, ownerName, phoneNo, address, country), created direct set Password Reset modal (bcrypt 10 rounds), protected endpoints with `manage:tenants` permission and added complete audit logs and unit test coverage. |
| **2026-09-14** | **Official Meta Tech Provider Branding & Logo Showcase** | `meta-tech-provider.png`, `MetaIntegrationSection.tsx`, `HomePageContent.tsx`, `SuccessStoriesContent.tsx`, `InteractiveFeatureTabs.tsx` | Updated all website branding and badges from "Meta Integrated" to "Official Meta Tech Provider". Integrated official `meta-tech-provider.png` logo asset with glowing card showcase, Portfolio ID details, and badges across home & success stories pages. |
| **2026-09-14** | **First-Month Promotional Discount Panel & Upcoming Bill Fix** | `payments.service.ts`, `subscription/page.tsx`, `billing-history/page.tsx` | Fixed display of promotional monthly discounts in tenant Subscription panel (promo display price, main card badge) and updated `getUpcomingBill()` API + Billing History Upcoming Bill card to correctly return and render discounted promo amounts & badges. |
| **2026-09-11** | **Instagram DM Meta Webhook Parsing & Channel Connection Match Fix** | `messenger.service.ts`, `project-context.md` | Updated `MessengerService.parseWebhookPayload` to match Instagram channel connections by `recipientId` and `senderId` in addition to `entryId`. Resolves issue where Instagram webhooks sent under Facebook Page object subscriptions (`object: page`) were failing connection lookup and not appearing in ZiniChat Inbox. |
| **2026-09-11** | **Vertical Mode Demo Request / Inquiry Order Auto-Creation Engine** | `orders.service.ts`, `orchestrator.service.ts`, `project-context.md` | Updated `OrdersService.createOrder` to support optional items & custom `createdBy: 'ai'` tag. Updated `OrchestratorService` to auto-create `Order` records in the database whenever AI receives vertical demo requests/inquiries, populating `https://zinichat.com/dashboard/orders`. Generated missing demo request order for tenant `job.nadim30@gmail.com`. |
| **2026-09-11** | **Instagram OAuth Discovery Enhancement & Multi-Token Fallback** | `instagram-auth.service.ts`, `ConnectFacebookInstagramButton.tsx`, `project-context.md` | Enhanced Instagram Business Account detection with user-token fallback, restored `instagram_basic` scope, and added detailed Meta Graph API response logging. |
| **2026-09-11** | **Meta Access Verification Approved (Tech Provider Verified) & Facebook Login Operational** | Meta Portal (`165766951997356`), `project-context.md` | Meta Access Verification form officially approved and status marked as **Verified** for Tech Provider under Business Portfolio `3833563216908598`. Facebook Login & Meta OAuth (WhatsApp / Messenger / Instagram DM) confirmed 100% operational live without restrictions. |
| **2026-09-10** | **Meta Developer Console Setup & Tech Provider Access Verification Matrix** | Meta Portal (`165766951997356`), `project-context.md` | Configured Valid OAuth Redirect URIs (`whatsapp`, `instagram`), Allowed JS SDK Domains (`https://zinichat.com/`), and submitted Access Verification (Tech Provider) form under Business Portfolio `3833563216908598`. Fully documented in context. |
| **2026-09-10** | **Deprecated Meta `instagram_basic` Scope Cleanup & Live Deployment** | `ConnectFacebookInstagramButton.tsx`, `project-context.md` | Removed deprecated `instagram_basic` parameter from frontend Facebook Login OAuth scope string in `ConnectFacebookInstagramButton.tsx` (Graph API v19+ merged it into approved `instagram_manage_messages`). Tested, committed (`3a67a64`), and deployed live to production. |
| **2026-09-10** | **Meta OAuth Granular Scope Minimal Fields Fallback & Live Deployment** | `messenger-auth.service.ts`, `instagram-auth.service.ts`, `project-context.md` | Added minimal field fallback (`fields=id,name`) in Path E of Messenger & Instagram auth services to recover granular target pages/accounts when Meta restricts `pages_read_engagement` scope. Tested, committed (`d028689`), and deployed live to production. |
| **2026-09-09** | **Live Server Package Settings Template Sync** | `scripts/sync-live-plans.js`, `project-context.md` | Inspected live DB via SSH; executed `sync-live-plans.js` to sync `Free` plan's 25-feature array and limits to `Starter`, `Growth`, and `Scale` plans. |
| **2026-09-09** | **High-Professional SEO Implementation for Worldwide Launch** | `next.config.ts`, `robots.ts`, `sitemap.ts`, `llms.txt`, `JsonLd.tsx`, `layout.tsx`, marketing pages, `opengraph-image.tsx` | Added compression/security headers, created `robots.ts`, `sitemap.ts`, `llms.txt`, per-page JSON-LD schemas (`Organization`, `SoftwareApplication`, etc.), and dynamic OpenGraph 1200x630 banner. |
| **2026-09-09** | **Price-Wise Plan Sorting, Currency Control (`$`/`৳`) & 4-Card Layout** | `PricingSection.tsx`, `pricing/page.tsx` | Sorted plans by numeric price & tier sequence (`Free` -> `Starter` -> `Growth` -> `Scale`), enforced strict currency switcher ($/৳), and updated layout grid to 4 side-by-side cards. |
| **2026-09-09** | **Weekly Billing Tab UX Optimization & Free Plan Trial Badge** | `PricingSection.tsx`, `pricing/page.tsx` | Dynamically hide Weekly tab if paid plans lack weekly billing, added "1-Week Free Trial" badge on Free plan card, auto-fallback to monthly view. |
| **2026-09-09** | **Outbound-Only Message Quota Tooltip Fix & Live MCP Deployment** | `PricingSection.tsx`, `pricing/page.tsx` | Clarified tooltip text to state outbound replies count towards quota while inbound customer messages are unlimited. Built, committed, and deployed live. |
| **2026-09-09** | **Yearly Sub-Period Quota Reset Engine, Usage Progress Bars & Badges** | `billing.service.ts`, `payments.service.ts`, `mfs-payments.service.ts`, `subscription/page.tsx`, `billing-history/page.tsx` | Built yearly sub-period 30-day quota reset slice for yearly plans, dynamic cycle math (365/30/7 days), usage progress bars, monthly quota reset date banners, and billing cycle badges. |
| **2026-09-09** | **Dynamic Info Tooltips for Top 3 Plan Features** | `PricingSection.tsx`, `pricing/page.tsx` | Added `lucide-react` `Info` hover tooltips with bilingual explanations for Team Members, Messages/mo, and AI Responses/mo. |
| **2026-09-09** | **Tenant Panel Pre-Live Audit & Hardening** | `quota.service.ts`, `billing.service.ts`, `subscription.guard.ts`, auth services | Updated 8 backend files to check `status: { in: ['active', 'trialing'] }`, fixed channel quota count filters (`status: active/connected`), and resolved effective team seat limits. Passed 70/70 test suites. |
| **2026-09-09** | **Bangla QR Dynamic EMVCo Checkout & Superadmin Client Brands Showcase** | `schema.prisma`, `client-brands.*`, `mfs-payments.*`, `sp@dmin/settings/client-brands/page.tsx`, `(marketing)/page.tsx` | Added EMVCo Bangla QR generator with Tag 63 Hex CRC-16, built Superadmin Client Brands CRUD & logo upload module, and updated landing page client brand marquee. |
| **2026-09-09** | **Official Meta Integrated Platform Showcase & Badges** | `MetaIntegrationSection.tsx`, `(marketing)/page.tsx`, `success-stories/page.tsx` | Created `MetaIntegrationSection` showcasing Meta Cloud/DM API compliance, 100% account safety, green tick readiness, and embedded in landing and success stories pages. |
| **2026-09-09** | **Free Plan One-Time Enforcement, Weekly Billing Engine & Superadmin Extension** | `schema.prisma`, `auth.service.ts`, `payments.service.ts`, `billing.service.ts`, `sp@dmin/packages/page.tsx`, `sp@dmin/billing/page.tsx` | Added `hasUsedFreePlan` to prevent free plan abuse, added configurable `allowWeekly` & `priceWeeklyBdt` to `Plan`, auto-assigned 7-day duration on signup, and added Superadmin subscription extension modal (+7/+14/+30/+90 days). |
| **2026-09-09** | **Superadmin Left Menu Instant Loading Fix & Navigation Token Sync** | `sp@dmin/ClientLayout.tsx`, `sp@dmin/login/page.tsx` | Re-evaluated JWT `access_token` and permissions on pathname changes, and used full-page redirect upon login to ensure immediate sidebar menu render. |
| **2026-08-31** | **Tenant Dashboard Client Runtime Error Fix & Error Boundary Integration** | `dashboard/page.tsx`, `SetupJourneyWidget.tsx`, `dashboard/error.tsx` | Added array/string type guards across stats and widgets, wrapped `localStorage` in try/catch, and integrated Next.js `error.tsx` error boundary. |
| **2026-08-31** | **Product Price Optional Across All Verticals & "Price on Call" UI** | `products/page.tsx` | Made price inputs optional across all 8 verticals, handled empty submissions safely, and rendered italicized "Price on Call / আলোচনা সাপেক্ষে" fallback. |
| **2026-08-31** | **Comprehensive 2px Vibrant Outlines Across All Dashboard Boxes** | `dashboard/page.tsx` | Applied 2px color-tinted outlines (`border-2`) across all dashboard cards/boxes, expanded grid for 5 KPI cards. Deployed live. |
| **2026-08-31** | **Light Mode Contrast Overhaul & Webhook Cards & AUTOMATION Sidebar** | `ClientLayout.tsx`, `inboxes/new/page.tsx` | Fixed dark mode text classes for light mode, added copyable webhook credentials cards for Messenger/WA/IG, renamed sidebar group to AUTOMATION with soft green glass background. |
| **2026-08-31** | **Meta App Review Screencast & Inbox Profile Picture Rendering** | auth services, `inbox.service.ts`, `inboxes/page.tsx` | Added visual cards with original Facebook/Instagram page profile pictures, enhanced setup guide, and deployed live via MCP. |
| **2026-08-21** | **Unverified User Re-Signup & Login OTP Recovery & Endpoint Fix** | `auth.service.ts`, `auth.controller.ts`, `signup/page.tsx`, `login/page.tsx` | Added missing `@Post('verify-otp')` and `resend-otp` endpoints, updated unverified re-signup to send fresh OTP instead of blocking, and clarified 15-min expiration label. |
| **2026-08-21** | **Email Flow Separation, HTML Template Sanitization & Business Nature** | `auth.service.ts`, `smtp.service.ts`, `business-nature.service.ts`, `signup/page.tsx` | Separated OTP email (sent on signup) from welcome email (sent post-OTP verification), created high-impact HTML OTP email template, and sorted "Others" business nature to last. |
| **2026-08-21** | **Strict Language Localization, Country Database & Blank Default** | `countryData.ts`, `signup/page.tsx`, `login/page.tsx`, `onboarding/page.tsx` | Enforced strict `useLanguage()` localization across auth forms, expanded world country list (120+ A-Z), and set country dropdown to blank default. |
| **2026-08-21** | **Google Auth Button Alignment & Mobile Width Fix** | `signup/page.tsx`, `login/page.tsx` | Centered Google Sign-In button and applied dynamic responsive width `Math.max(200, Math.min(availableWidth, 380))` for mobile screens. |
| **2026-08-21** | **Country Selection & Auto Phone Prefix, Locked Profile Country & Setup Banner** | `schema.prisma`, `countryData.ts`, `tenants.controller.ts`, `SetupJourneyWidget.tsx` | Added `country` to `Tenant`, updated auto-dial code prefixing, added locked country display in user profile with Superadmin edit endpoint, and made 3-step setup banner persistent until complete. |
| **2026-08-21** | **Signup Form 6-Digit Email OTP Flow & Blank Business Nature Default** | `auth.dto.ts`, `auth.service.ts`, `auth.controller.ts`, `signup/page.tsx` | Enforced blank business nature default, built 6-digit OTP verification flow with 15-min expiration and resend countdown timer (60s). |
| **2026-08-21** | **Signup Form DTO Whitelist Fix, Password Eye Toggle & Onboarding Fields** | `auth.dto.ts`, `auth.service.ts`, `signup/page.tsx` | Added missing fields (`fullName`, `brandName`, `address`, etc.) to `SignupDto` fixing NestJS ValidationPipe errors, added password visibility eye toggles, and unified onboarding fields. |
| **2026-08-19** | **Universal Voice/Audio Transcription (Whisper & Groq Free Tier, Storage & Superadmin UI)** | `ai.service.ts`, `inbox.service.ts`, `whatsapp-web.service.ts`, `sp@dmin/settings/ai/page.tsx` | Built voice transcription using free Groq Whisper (`whisper-large-v3`, 7,000 req/day) with OpenAI fallback, skipped Whisper if AI auto-reply disabled, tracked storage usage, and updated Superadmin AI UI. Passed 62 unit tests. |
| **2026-08-18** | **Superadmin Tenant Report Link Logout Bug Fix** | `middleware.ts`, `tenants.controller.ts` | Fixed base64url decoding in `middleware.ts` to replace `-`/`_` and pad `=`, preventing `DOMException` from logging out Superadmins when clicking Tenant Report links. |
| **2026-08-18** | **Superadmin Universal Pagination & Standardized Loading States (`AdminLoader`)** | `AdminPagination.tsx`, `AdminLoader.tsx`, `sp@dmin/*` | Built reusable `AdminPagination` with page size selection (`10`, `20`, `50`, `100`) and dual-theme `AdminLoader` skeleton across all 15 Superadmin list pages. |
| **2026-08-18** | **Superadmin Light Mode Font & Contrast Audit & Dual-Theme Fix** | `sp@dmin/*` list pages | Replaced hardcoded dark classes with adaptive Tailwind pairs (`text-slate-900 dark:text-zinc-100`, `bg-white dark:bg-surface`) across tables, headers, and modals on all Superadmin pages. |
| **2026-08-18** | **Superadmin Team Granular Access Permissions & Working Checkboxes** | `sp@dmin/team/page.tsx`, `ClientLayout.tsx`, `permissions.guard.ts` | Replaced div toggles with standard checkboxes for 20+ granular permissions in 6 categories, and aligned backend `PermissionsGuard` and sidebar checks. |
| **2026-08-18** | **Superadmin Dashboard Platform Overview Data & Mobile Optimization** | `stats.service.ts`, `sp@dmin/page.tsx` | Added `pendingPaymentsCount`/`monthAiResponses` metrics, pending payment banner, tenant health widget (4 metrics), interactive card links, and responsive grid layouts. |
| **2026-08-18** | **Superadmin Billing & Subscriptions Financial Operations Overhaul** | `billing.service.ts`, `sp@dmin/billing/page.tsx` | Added MRR, Total Collected, Pending Collections, and Expiring Soon KPI widgets, filter tabs, live search, and transaction history ledger tab. |
| **2026-08-18** | **Superadmin Pending Payment Approval Confirmation Modal** | `sp@dmin/payments/page.tsx` | Added double-confirmation modal displaying Tenant Name, Plan Name, BDT Amount, and TrxID before activating pending manual payments. |
| **2026-08-18** | **Tenant Report Support Tickets, Support AI Chats & Interactive Link Cards** | `tenants.service.ts`, `sp@dmin/tenants/[id]/page.tsx` | Added total support tickets and AI support widget sessions metrics, and converted summary cards into interactive deep links to Superadmin pages. |
| **2026-08-18** | **Tenant Report AI Response Label, AI Tokens Used & Ecosystem Summary** | `tenants.service.ts`, `sp@dmin/tenants/[id]/page.tsx` | Renamed label to "AI Response", added aggregate LLM token consumption (`tokensUsed`), and expanded ecosystem widget to 6 metrics (members, channels, products, leads, FAQs, crawls). |
| **2026-08-18** | **Superadmin Tenant Impersonation Fix & Exit Banner Integration** | `sp@dmin/tenants/page.tsx`, `sp@dmin/impersonate/page.tsx`, `ClientLayout.tsx` | Fixed middleware redirect by setting `user_role` cookie during impersonation, and added floating "Exit Workspace" banner in tenant workspace layout. |
| **2026-08-18** | **Marketing Header Navbar & Mobile Menu Home Button Integration** | `layout.tsx` (marketing) | Added explicit "Home / হোম" link button to desktop navigation bar and mobile dropdown menu. |
| **2026-08-18** | **ZiniChat 17 Unique Selling Points (USP) Integration** | `landing-page.service.ts`, `InteractiveFeatureTabs.tsx`, `features/page.tsx`, `page.tsx` | Updated features/FAQs JSON with 13 rich features and 16 bilingual Q&As, added interactive feature mockups (RAG, Multi-Vertical, MFS, BYOK), and updated homepage hero stats. |
| **2026-08-17** | **Message & AI Quota Warnings (80% & 100% Alerts, Resets, Emails & Banners)** | `schema.prisma`, `quota.service.ts`, `smtp.service.ts`, `billing.service.ts`, `ClientLayout.tsx` | Added notification flags to `Tenant`, created 4 customizable SMTP templates, and added amber/red floating warning banners at >=80% and >=100% usage. |
| **2026-08-17** | **Storage Quota Warnings (80% & 100% Full Notifications & Layout Banner)** | `schema.prisma`, `quota.service.ts`, `smtp.service.ts`, `ClientLayout.tsx` | Added storage warning flags, customizable email templates, and dashboard warning/block banners. Passed 32 unit tests. |
| **2026-08-17** | **New Success Stories Page with 6 Industry Modeled Results** | `ResultsSection.tsx`, `success-stories/page.tsx` | Built ROI success stories page with top-stat bar, F-Commerce case study, and 6 industry modeled scenarios (Fashion, Restaurant, Real Estate, Diagnostics, Wholesale, Logistics). |
| **2026-08-17** | **Secure Login Logging & Retention System (90-day TTL, Superadmin-only)** | `schema.prisma`, `login-logs.*`, `security-logs/page.tsx` | Created `login_logs` table capturing client IP, user-agent, location, status, fail reason; added non-blocking async logging, 90-day auto-purge cron, and Superadmin UI. |
| **2026-08-17** | **Geolocation Language & Currency Auto-Detection** | `LanguageProvider.tsx`, `CurrencyProvider.tsx` | Auto-detects BD IP/timezone -> Bengali (`bn`) & BDT (`৳`); Non-BD IP -> English (`en`) & USD (`$`), with `localStorage` preference fallback. |
| **2026-08-17** | **Bengali Typography Upgrade: Noto Sans Bengali Integration** | `layout.tsx`, `globals.css` | Integrated `Noto_Sans_Bengali` font across system to eliminate Bengali glyph distortion on characters like 'এ', 'এক', 'এআই'. |
| **2026-08-17** | **CSV Contact Import, Sample Format Download & 4-Step Guidelines** | `contacts.service.ts`, `contacts.controller.ts`, `broadcasts/page.tsx` | Built sample CSV downloader, bulk contact importer with phone normalizer (Bengali digits `০-৯` to `+8801`), drag-and-drop modal, and 4-step guideline banner. |
| **2026-08-17** | **Meta WhatsApp Cloud API v25.0 Marketing Messages API & Fallback** | `whatsapp.processor.ts` | Routed marketing templates to `POST /v25.0/{phone-number-id}/marketing_messages` with automatic fallback to standard `/messages` endpoint on permission error. |
| **2026-08-17** | **Support Ticket Real-time Reply Notifications & Sidebar Badges** | `tickets.service.ts`, `notifications.service.ts`, `NotificationBell.tsx`, `ClientLayout.tsx` | Added ticket socket notifications, real-time toast alerts on new replies, and red unread counter badges on sidebar menu items. |
| **2026-08-17** | **AI Phone Number Auto-Extraction & Multilingual Digit Converter** | `inbox.service.ts` | Built `extractPhoneFromText` supporting English/Bengali digits (`০-৯`), hyphens, and `+8801` prefixes; updates contact phone and emits WebSocket events in real-time. |
| **2026-08-17** | **Meta Profile Sync, Real-time Outbound AI DM Broadcast & Contact Cleanup** | `inbox.service.ts`, `inbox/page.tsx` | Broadcasts outbound AI DM `new_message` events in real-time, syncs customer names from Meta Graph API, and separates phone numbers from external PSID/IGSID IDs. |
| **2026-08-17** | **FB Comments Lead CRM Box, Unread Notification Badge & Private DM Sync** | `inbox/page.tsx`, `facebook-comments.service.ts` | Added unread badge counter on FB Comments filter tab, rendered right-side lead CRM sidebar, synced private comment DMs into Messenger inbox, and handled test payloads. |
| **2026-08-17** | **Facebook Page Comment Automation & Webhook Subscription Fix** | `instagram-auth.service.ts`, `messenger-auth.service.ts`, `facebook-comments.service.ts` | Removed invalid parameter `instagram_messaging_seen`, fixed channel connection lookup queries using `OR: [{ externalAccountId }, { verifyToken }]`, and enabled comment auto-reply by default. |
| **2026-08-17** | **Instagram DM Reply Endpoint & Page Token Fix** | `messenger.processor.ts`, `instagram-auth.service.ts` | Fixed Instagram DM delivery by targeting `POST /{PAGE_ID}/messages` endpoint (using stored Page Access Token in `accessTokenEncrypted`) instead of IG user ID endpoint. |
| **2026-08-17** | **Product Dynamic Search Schema Field Fix & Defensive Error Guards** | `ai.service.ts` | Replaced invalid `category` field in `Product.findMany` with valid fields (`name`, `sku`, `description`, `location`), and wrapped RAG search methods in try/catch. |
| **2026-08-17** | **Website Data Fetch & AI Training Tool (2 Quota Deduction & Confirmation Modal)** | `schema.prisma`, `website-crawler.service.ts`, `ai-training.service.ts`, `orchestrator.service.ts` | Built recursive website HTML text crawler (up to 12 pages, 25k chars limit), generated structured summary via LLM, deducted 2 AI Response credits, injected into system prompt, and added confirmation modal. |
| **2026-08-17** | **AI Token Optimization, Prompt Caching Architecture & Dynamic Tool Indexing** | `ai.service.ts`, `orchestrator.service.ts` | Restructured system prompts into Static Header (placed at top for 100% prompt cache hits) and Dynamic Footer (at bottom). Built Stage 0 indexing (`searchRelevantProducts`, `searchRelevantQnas`, max 5) and skipped vector search on generic greetings ("hi", "salam"). |
| **2026-08-17** | **Quota Carry Forward Engine & Homepage FAQ Update** | `schema.prisma`, `billing.service.ts`, `payments.service.ts`, `subscription/page.tsx` | Added `carriedForwardAiQuota` and `carriedForwardMessageQuota` to `Subscription`. Unused quotas on paid plan renewals now carry forward to next period. |
| **2026-08-16** | **Facebook Comments Live Inbox Multi-Reply & Socket Thread System** | `schema.prisma`, `facebook-comments.service.ts`, `inbox/page.tsx` | Added `replyHistory` JSON field, broadcast real-time `facebook_comment` socket events, supported public/private/both human multi-reply modes, and rendered chat thread view. |
| **2026-08-16** | **Full System Audit & Mobile Viewport / Feature Lock Hardening** | `ClientLayout.tsx`, `ai-training/page.tsx`, `team/page.tsx`, `whatsapp-web.service.spec.ts` | Unlocked Channel Integration menu for all tenants, added `max-h-[90vh]` modal scroll bounds, and verified 100% test pass rate across 68 backend test suites (505 tests). |
| **2026-08-11** | **Superadmin Tenant Impersonation System ("Enter Tenant Panel")** | `tenants.service.ts`, `sp@dmin/tenants/page.tsx`, `sp@dmin/impersonate/page.tsx` | Built silent Superadmin impersonation feature generating 2-hour JWTs, logging `SUPERADMIN_IMPERSONATED_TENANT` to `audit_logs`, and opening tenant workspace in new tab. |
| **2026-08-11** | **Support AI Prompt Caching Fix — Vertical-Aware Per-Vertical Cache** | `support-chat.service.ts`, `ai-cache.service.ts` | Fixed 3 Support AI cache bugs, creating vertical-aware system prompt cache keys across Gemini & OpenAI to reduce per-message token costs by ~78%. |
| **2026-08-11** | **Business Nature-Aware Support AI Training** | `support-chat.service.ts` | Added vertical-specific terminology context blocks (`resolveBusinessNatureContext`) across all 9 verticals + retail for ZiniChat Support AI. |
| **2026-08-10** | **Vector Embedding Dimension Mismatch Fix (768-dim Gemini Embeddings)** | `schema.prisma`, `ai.service.ts`, `ai-training.service.ts` | Updated `KnowledgeChunk.embedding` from `vector(1536)` to `vector(768)` (Gemini `text-embedding-004`), recreated HNSW index, and updated embedding generation. |
| **2026-08-10** | **TrxID Case Sensitivity & Trimming Fix for MFS Payments** | `mfs-payments.service.ts`, `SmsReceiver.kt` | Changed TrxID verification lookup to case-insensitive `findFirst({ trxId: { equals, mode: 'insensitive' } })` and trimmed whitespace. |
| **2026-08-10** | **Subscription & Quota Guards on Background AI Webhook Handlers** | `quota.service.ts`, `orchestrator.service.ts` | Added `isTenantSubscriptionActive` guard in `OrchestratorService` to drop incoming webhook messages silently for expired/suspended tenants. |
| **2026-08-10** | **API Throttling & Infinite Bot Loop Prevention** | `app.module.ts`, `inbox.service.ts` | Configured NestJS throttler (100 req/min global, 300 req/min webhooks) and built `checkBotLoopSafeguard` (disables AI auto-reply if 5 AI messages sent in 30s). |
| **2026-08-10** | **Offline Local Buffering for Android SMS Gateway** | `SmsBufferManager.kt`, `PendingSmsSyncWorker.kt` | Added WorkManager `PendingSmsSyncWorker` to buffer un-synced MFS SMS in SharedPreferences when offline and auto-flush to webhook on reconnect. |
| **2026-08-10** | **Baileys Auto-Reconnect & Socket Cleanup** | `whatsapp-web.service.ts` | Built exponential backoff reconnect strategy (up to 5 attempts), halted loops on code 440/409 socket conflicts, and dispatched socket drop alerts to admins. |
| **2026-08-06** | **ZiniChat Multi-Vertical 8: Logistics & Infrastructure (`isLogisticsMode`)** | `schema.prisma`, `orchestrator.service.ts`, `products/page.tsx`, `orders/page.tsx` | Implemented logistics mode (`shipment_quote_request` intent, route pricing, fleet capacity, "Shipments & Bookings" orders label, `Truck` icon). |
| **2026-08-06** | **ZiniChat Multi-Vertical 7: Manufacturing & Industrial (`isManufacturingMode`)** | `schema.prisma`, `orchestrator.service.ts`, `products/page.tsx`, `orders/page.tsx` | Implemented manufacturing mode (`bulk_rfq_inquiry` intent, MOQ, wholesale pricing, "RFQ / Quotations" orders label, `Factory` icon). |
| **2026-08-06** | **ZiniChat Multi-Vertical 6: Education & Academies (`isEducationMode`)** | `schema.prisma`, `orchestrator.service.ts`, `products/page.tsx`, `orders/page.tsx` | Implemented education mode (`course_admission_inquiry` intent, course duration/schedule, "Admissions" orders label, `GraduationCap` icon). |
| **2026-08-06** | **ZiniChat Multi-Vertical 5: Healthcare & Clinics (`isHealthcareMode`)** | `schema.prisma`, `orchestrator.service.ts`, `products/page.tsx`, `orders/page.tsx` | Implemented healthcare mode (`appointment_request` intent, doctor visiting hours/fees, "Appointments" orders label, `Stethoscope` icon). |
| **2026-08-06** | **ZiniChat Multi-Vertical 4: Financial Services (`isFinancialServiceMode`)** | `schema.prisma`, `orchestrator.service.ts`, `products/page.tsx`, `orders/page.tsx` | Implemented financial mode (`consultation_request` intent, service package fees, "Consultations" orders label, `Briefcase` icon). |
| **2026-08-06** | **ZiniChat Multi-Vertical 3: Technology & Software (`isTechSoftwareMode`)** | `schema.prisma`, `orchestrator.service.ts`, `products/page.tsx`, `orders/page.tsx` | Implemented tech mode (`demo_request` intent, software tiers/demo links, "Demo Requests" orders label, `Cpu` icon). |
| **2026-08-06** | **Superadmin Login Route Obfuscation to `/sp@dmin`** | `middleware.ts`, `sp@dmin/*` | Renamed superadmin routes from `/superadmin` to `/sp@dmin` for security obfuscation. |
| **2026-08-04** | **Facebook Comment Automation (Public/Private AI Replies & Config Modal)** | `schema.prisma`, `facebook-comments.service.ts`, `CommentConfigModal.tsx` | Built comment auto-reply engine (public/private/both modes, rate limiting, keyword filters, credit deduction safety) and dark glassmorphic config modal. |
| **2026-08-04** | **Meta Pixel & CAPI Parameter Builder + Google Analytics 4 Integration** | `meta-pixel.*`, `google-analytics.*`, `acquisition/track` | Added `fbc`/`referrerUrl` support to Meta CAPI, created public GA4 config endpoint, fixed GA `client_id` session consistency, and passed all test suites. |
| **2026-08-03** | **Password Reset Email Dispatch & Case-Insensitive Lookup** | `auth.service.ts`, `smtp.service.ts` | Fixed forgot-password email delivery with case-insensitive search (`mode: 'insensitive'`), synchronous direct mail dispatch, and frontend URL resolution. |
| **2026-08-03** | **PWA Push Notifications & Installation Banner** | `push-notification.service.ts`, `sw.js`, `PwaInstallBanner.tsx` | Added PWA manifest, install banner, VAPID Web Push notifications (AES payload encryption), service worker registration, and profile push settings toggle. |
| **2026-07-31** | **Smart Storage Manager & VPS Disk Unlinking** | `storage.service.ts`, `storage/page.tsx` | Built 4-category storage API breakdown, physical VPS disk unlinking (`fs.promises.unlink`), progress bar UI, and 30d/90d cleanup actions. |
| **2026-07-31** | **Strict Anti-Hallucination Guardrails & AI Safety Pipeline** | `orchestrator.service.ts`, `ai-training.service.ts` | Increased confidence threshold to 0.8, added 60-day document freshness cutoff, enforced explicit order confirmation keywords (`CONFIRM`), and capped tags to 10. |
| **2026-07-31** | **Inbox 6 Major Upgrades (Lightbox, Quote & Reply, Forward, Block, Stage Selector)** | `inbox.service.ts`, `inbox/page.tsx`, `ConversationSidebar.tsx` | Built lightbox image download, message quote & reply preview, message forwarding modal, contact blocking, inline CRM stage selector, and 50/50 parameter filter bar. |
| **2026-07-30** | **Inbox AI Event-Wise Behavior Toggles & Structured Classification** | `orchestrator.service.ts`, `ai-training/page.tsx` | Implemented 2-stage LLM classification + deterministic code handlers for `order_placement`, `image_reading`, `support_detection`, `product_matching` with UI feature locks. |
| **2026-07-30** | **Inbox CRM Upgrade (Collaborators, Activity Logs, Presence, Smart Tabs)** | `inbox.service.ts`, `activity-log.service.ts`, `user-presence.service.ts`, `inbox/page.tsx` | Added starred/archived/resolved conversation flags, collaborator assignments, activity trail, user presence status, Smart Tabs bar, and 7-section CRM right sidebar. |
| **2026-07-29** | **Full Production Readiness Audit & Certification (25 / 25 Phases — 100%)** | `testing-reports/*` | Completed full-platform code-trace audit across all 25 phases. Passed NestJS build (0 errors), NestJS unit tests (377/377 passed), frontend typecheck (0 errors), frontend tests (6/6 passed). |
| **2026-07-28** | **ZiniChat Enterprise Support AI Engine & Session Management** | `support-chat.service.ts`, `SupportWidget.tsx` | Built Support AI with real-time tenant context injection, function calling tools (`create_support_ticket`, `navigate_to_page`), session auto-closure (>30m), and prompt editor. |
| **2026-07-28** | **Executive BI Dashboard & Date Range Filters** | `tenant-stats.service.ts`, `dashboard/page.tsx` | Built Executive BI Dashboard with 6 KPI cards, 0-100 business health score, area/bar/pie charts, and YouTube-style date range filters (`today`, `7d`, `15d`, `30d`, `90d`, `this_month`). |
| **2026-07-28** | **Inboxes Real-Time Status & Reconnect Button** | `inbox.service.ts`, `whatsapp-web.service.ts`, `settings/inboxes/page.tsx` | Added real-time dynamic channel status (`Active 🟢` vs `Disconnected 🔴`), reconnect/scan QR CTA button, and AI auto-reply toggle endpoint `PATCH /inbox/channels/:id/ai-reply`. |

---

## 5. Meta Developer Portal & Ecosystem Configuration Matrix

This section documents the exact state of Meta Developer Apps, Business Portfolios, OAuth Redirect URIs, Permissions, and Access Verifications:

* **Meta App Details**:
  * **App Name**: ZiniChat Platform
  * **App ID**: `165766951997356`
  * **App Type**: Business
  * **App Mode**: Live (Toggle ON)
  * **Business Portfolio**: BariChai (`3833563216908598`)

* **Facebook Login for Business -> Settings Configurations**:
  * **Valid OAuth Redirect URIs**:
    - `https://zinichat.com/dashboard/settings/whatsapp`
    - `https://zinichat.com/dashboard/settings/instagram`
  * **Allowed Domains for the JavaScript SDK**:
    - `https://zinichat.com/`
  * **OAuth Switches**: Client OAuth Login (YES), Web OAuth Login (YES), Enforce HTTPS (YES), Login with JS SDK (YES).

* **Permissions & Scopes**:
  * `instagram_manage_messages` (Approved & Published in App Review)
  * `pages_show_list`, `pages_read_engagement`, `pages_manage_metadata`, `pages_messaging`
  * *Note*: Deprecated `instagram_basic` scope removed from frontend (`ConnectFacebookInstagramButton.tsx`) as Graph API v19+ merged it into `instagram_manage_messages`.

* **Access Verification (Tech Provider)**:
  * **Status**: **VERIFIED / APPROVED** (Verified on Sep 11, 2026).
  * **Business Role**: Verified Tech Provider / SaaS Platform providing automated omnichannel messaging & AI live inbox service for client businesses.
  * **Scope & Impact**: All public accounts (non-testers included) can connect Facebook Pages, Instagram Accounts, and WhatsApp Cloud API seamlessly with 100% un-restricted access via Facebook Login.

* **Backend Resiliency Guard (Path E Fallback)**:
  * Catch Meta `#100 missing pages_read_engagement` error when tokens are restricted, execute minimal query (`fields=id,name`), and assign `access_token = userToken` to recover target pages/accounts (`messenger-auth.service.ts` & `instagram-auth.service.ts`).

---

## 6. Project Directory Structure

```
ZiniChat/
├── .agents/                      # Workspace Agent Instructions & Custom Rules
│   └── AGENTS.md                 # Primary Agent Rules & Execution Guidelines
├── android-sms-gateway/          # Kotlin Native Android App for MFS SMS Ingestion
│   └── app/src/main/             # Android Receiver, WorkManager, Buffer & Manifest
├── backend/                      # NestJS Modular Monolith Backend
│   ├── prisma/                   # Database Schema (`schema.prisma`) & Migrations
│   └── src/
│       ├── ai/                   # LLM SDK Integrations & Vector RAG Engine
│       ├── ai-training/          # Persona Setup, Q&A Training & Live Simulator Agent
│       ├── audit-logs/           # Superadmin Audit Logging Module
│       ├── auth/                 # JWT Auth, OTP Verification, Google OAuth, BYOK
│       ├── billing/              # Subscriptions, Quota Reset Engine & Plan Limits
│       ├── broadcasts/           # Segmented Outbound Campaign Engine & BullMQ Queue
│       ├── business-nature/      # 8-Vertical Mode Configuration Engine
│       ├── channels/             # WhatsApp (Cloud & Baileys), Messenger, Instagram DM APIs
│       ├── client-brands/        # Superadmin Client Brand Marquee Showcase
│       ├── common/               # Shared Guards, Decorators, Interceptors & Filters
│       ├── contacts/             # CRM Leads Engine, CSV Import & Phone Normalizer
│       ├── crypto/               # AES-256-CBC Secret Encryption Helpers
│       ├── currency/             # Multicurrency (BDT ৳ / USD $) Provider
│       ├── file-validation/      # Buffer Inspection, Magic Bytes & Extension Guard
│       ├── google-analytics/     # GA4 Measurement Protocol v2 Server-to-Server CAPI
│       ├── inbox/                # Realtime Omnichannel Live Inbox & Socket.IO Gateway
│       ├── inquiries/            # Platform Sales & Demo Inquiries Module
│       ├── labels/               # CRM Tags, Labels & Prompt Rules Engine
│       ├── landing-page/         # Public Features & FAQs Management
│       ├── leads/                # Multi-Vertical Kanban Lead Stages
│       ├── login-logs/           # Secure Login Audit Log & 90-Day Purge Cron
│       ├── meta-pixel/           # Meta CAPI Acquisition Event Dispatcher
│       ├── mfs-payments/         # bKash/Nagad/Rocket EMVCo Bangla QR & SMS Gateway
│       ├── notifications/        # Realtime WebSockets, Emails & Team Lead Alerts
│       ├── orchestrator/         # Inbox AI 2-Stage Classifier & Prompt Caching Engine
│       ├── orders/               # E-Commerce & Multi-Vertical Booking Engine
│       ├── packages/             # Superadmin Package & Plan Management
│       ├── payments/             # Manual, Sandbox & Auto MFS Subscriptions
│       ├── products/             # Tenant Catalog & Dynamic Attributes Store
│       ├── smtp/                 # SMTP Email Dispatcher & Custom Alert Templates
│       ├── stats/                # Tenant Executive BI & Superadmin Analytics
│       ├── storage/              # Smart File Storage & VPS Disk Unlinking
│       ├── support-chat/         # Platform Support AI Agent (`SupportChatService`)
│       ├── team/                 # Team Roles, Granular Permissions & Seat Limits
│       ├── tenants/              # Tenant Isolation, Workspace Customization & Impersonation
│       ├── tickets/              # Realtime Support Ticket System
│       ├── users/                # User Accounts, Roles & Profile Management
│       └── website-widget/       # Web Embed Live Chat Widget Backend
├── frontend/                     # Next.js 16 App Router Frontend
│   ├── public/                   # Static Assets, PWA Icons, `llms.txt`
│   └── src/
│       ├── app/
│       │   ├── (auth)/           # Login, Signup, OTP Verification, Forgot Password
│       │   ├── (marketing)/      # Landing Page, Pricing, Features, FAQ, Success Stories, Legal
│       │   ├── (tenant)/         # Tenant Dashboard, Live Inbox, CRM, Settings, AI Training
│       │   ├── api/              # PWA Manifest, Analytics & Acquisition Endpoints
│       │   └── sp@dmin/          # Obfuscated Superadmin Operations Workspace
│       ├── components/           # Glassmorphic UI Components, Modals, SEO (JsonLd)
│       ├── context/              # Language (EN/BN), Currency (BDT/USD), Notification Contexts
│       ├── hooks/                # `useLanguage()`, `useCurrency()`, `useFeature()`
│       ├── lib/                  # API Clients, Socket Listeners & Formatting Utilities
│       └── middleware.ts         # JWT Session Guard, Role Router & Security Obfuscation
├── nginx/                        # Nginx Proxy Configurations
├── redis-local/                  # Local Redis Cache Configs
├── scripts/                      # Operations & MCP Deployment Pipeline
│   ├── invoke-mcp.js             # Deployment Script (`node scripts/invoke-mcp.js <target> <branch>`)
│   ├── sync-live-plans.js        # Live Server Package Settings Template Sync
│   └── check-*.js                # Diagnostics, Health Checks & Log Fetching Tools
├── supabase/                     # Supabase Migration & Vector DB Tools
└── docker-compose.yml            # Multi-Container Deployment Orchestration (Live & Test)
```

---

## 7. Operations & Next Steps
- **Active Operations**: Live system maintenance, package template synchronization, and user onboarding monitoring.
- **Deployment Protocol**: Always use `node scripts/invoke-mcp.js live main` after local testing and git commit/push.
