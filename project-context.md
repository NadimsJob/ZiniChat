# Project Context & Developer Log

This file serves as the living memory and source of truth for the **Omnichannel AI Business Assistant Platform**. Every AI agent or developer working on this project must read this file at the start of a task and update it immediately after any architecture changes, feature implementations, or major updates.

---

## 1. Project Overview
A multi-tenant SaaS platform enabling businesses (tenants) to manage customer communication via a unified inbox and self-serve AI Assistants across WhatsApp, Meta Messenger, and Instagram DM.
* **Primary Architecture Spec**: [full-platform-architecture-v3.md](file:///f:/AI%20Assistant%20SAAS/full-platform-architecture-v3.md)

---

## 2. Tech Stack & Environment
* **Frontend**: Next.js 16 (App Router) + TypeScript + Tailwind CSS (Unified Glassmorphism Theme)
* **Backend**: Node.js + NestJS (Modular Monolith)
* **Database**: PostgreSQL with `pgvector` extension (HNSW index optimized)
* **Queue/Cache**: Redis + BullMQ
* **Realtime**: Socket.io (namespaces for `/inbox` and `/notifications`)
* **AI Layer**: OpenAI, Anthropic Claude, & Google Gemini SDKs (BYOK & Platform-key mode)
* **Deployment**: Docker Compose + Traefik Reverse Proxy + SSL certificates (Live: `zinichat.com`, Staging: `test.zinichat.com`)

---

## 3. Current Status & Active Focus
- **Active Focus**: AI Image Vision Analysis, Mobile UI Native App Responsiveness, Universal Interactive Instruction Banners, and Live Inbox Layout Optimization.
- **Recent Implementations**:
  - **WhatsApp Web Session Persistence & Outbound Delivery Fix**: Mounted persistent Docker volume `zinichat_backend_sessions:/usr/src/app/sessions` in `docker-compose.yml` to preserve Baileys `creds.json` across container restarts/deploys. Updated `WhatsappProcessor` & `inbox.service.ts` to automatically resolve active `channelConnectionId` for re-paired WhatsApp connections, and sanitized recipient JID formatting.
  - **Dashboard Quota Breakdown & Visual Contrast**: Aligned AI quota calculations to `periodStart` (billing cycle start), changed terminology to "AI Response Usage", and added `shadow-lg shadow-black/5 hover:shadow-xl` styling across all dashboard cards for visual contrast.
  - **AI Image Vision & Catalog Matching**: Implemented AI image reading in Live Inbox (`orchestrator.service.ts` & `ai.service.ts`). AI automatically analyzes customer-submitted product photos, matches items against tenant Product Catalog (name, price in BDT, availability), deducts **5 AI credits** per image analysis via 5 `aiUsageLog` entries, and provides text-only fallback (using caption + product catalog) when a model without Vision support is selected.
  - **Vision Model Indicators & UI Badges**: Added live `Vision Supported` vs `Text Only` badges across Superadmin AI Configuration cards/modals, Tenant AI Settings, and Live Inbox message bubbles. Passed 138/138 backend unit tests (including complete AI image vision unit tests for multi-provider support).
  - **Live Inbox Overhaul**: Enforced 3-column desktop layout (`flex flex-col md:flex-row h-full`), mobile single-pane flow with `< Back` button, sticky pinned header, gradient emerald chat bubbles, compact Lead Details pane (`w-52/w-56`), and default `showLeadInfo: false`.
  - **Workspace Rules**: Added mandatory rule requiring all web/desktop design changes to be simultaneously optimized for mobile native app responsiveness.

---

## 4. Implementation History Log

| Date | Feature / Module | Affected Files | Key Details & Architectural Notes |
|---|---|---|---|
| **2026-07-28** | **WhatsApp Outbound Message Delivery & Channel Connection Resolution Fix** | `whatsapp.processor.ts`, `inbox.service.ts`, `whatsapp-web.service.ts` | Fixed WhatsApp outbound messaging from Live Inbox: (1) Updated `WhatsappProcessor` connection lookup so that if `channelConnectionId` in conversation points to a deleted/stale connection ID (e.g. from QR re-pairing), it automatically falls back to finding the tenant's current active connection (`status: 'active'`). (2) Updated `inbox.service.ts`'s `saveOutboundMessage` to automatically resolve and persist active `channelConnectionId`. (3) Sanitized Baileys target recipient JID (`cleanNumber = to.split('@')[0].split(':')[0].replace(/\D/g, '')`). Deployed to Live (`2b5b2cc`). All 186/186 backend unit tests passing. 0 TS errors. |
| **2026-07-28** | **WhatsApp Web Session Persistence & Docker Volume Fix** | `docker-compose.yml`, `whatsapp-web.service.ts` | Fixed WhatsApp Web connection loss after logout/re-login/container restart: (1) Added `zinichat_backend_sessions:/usr/src/app/sessions` volume to Docker compose so Baileys session files (`creds.json`) persist across container updates/restarts. (2) Added `keepAliveIntervalMs: 25000` to prevent TCP socket timeouts. (3) Added socket fallback auto-reconnect in `sendMessage` and background reconnect handling. Deployed to Live (`32162e4`). 9/9 unit tests passing. 0 TS errors. |
| **2026-07-28** | **Dashboard Quota Breakdown Alignment, Card Shadows & Product Catalog Limit** | `dashboard/page.tsx`, `tenant-stats.service.ts`, `quota.service.ts`, `products.service.ts`, `schema.prisma`, `packages/page.tsx`, `tenants/page.tsx` | Fixed Dashboard quota calculation & UI: (1) Changed label from "AI Credits Usage" to "AI Response Usage" / "এআই রেসপন্স কোটা". (2) Aligned `ai.used` in `subscriptionHealth` to use `periodStart` (subscription cycle) instead of 30-day filter, and enforced `finalMessagesUsed = Math.max(messagesUsed, aiUsedInPeriod)` to guarantee mathematical consistency. (3) Added `productCatalogLimit` & `contactsLimit` to schema & plan models, enforced `checkProductCatalogQuota` in `ProductsService`, and dynamically hide Contacts row when limit is null (unlimited). (4) Added `shadow-lg shadow-black/5 hover:shadow-xl` to all dashboard section cards for visual contrast against background. Deployed to Live (`c0e8c53`, `0f98965`). 29/29 unit tests passing. 0 TS errors. |
| **2026-07-28** | **Live Inbox 5 Key Enhancements & Outbound Image Fixes** | `ClientLayout.tsx`, `inbox/page.tsx` | Implemented 5 Live Inbox user requests: (1) Added real-time bottom-right toast notification popup with contact name, message snippet, and quick reply link when user is outside `/dashboard/inbox`. (2) Fixed outbound image rendering by adding `parseMessageContent()` helper to extract `mediaUrl` and avoid displaying raw JSON string `{body, mediaUrl}`. (3) Restricted AI Vision badge display strictly to inbound customer messages (`!isOutbound`). (4) Changed terminology from "AI Credit" to "AI Response" / "এআই রেসপন্স". (5) Created auto-collapsible sidebar for `/dashboard/inbox` route (`w-[56px]`) with expand-on-hover and pin toggle. Kept strictly local per user request. All 23 unit tests passing. Zero TS errors. |
| **2026-07-28** | **WhatsApp Web Auto-Redirect Fix After Connection** | `whatsapp-web.service.ts`, `inboxes/new/page.tsx` | Fixed WhatsApp Web QR scan redirect: (1) Updated `whatsapp-web.service.ts` to emit both `whatsapp_qr_connected` and `whatsapp_connected` events on connection open. (2) Updated `inboxes/new/page.tsx` to listen for both socket events and added a 2.5s status polling fallback. (3) Automatically triggers `router.push('/dashboard/settings/inboxes')` 1.8s after connection opens, redirecting the user back to the inbox list. Kept strictly local (deployment withheld per user request). All 23 unit tests passing. Zero TS errors. |
| **2026-07-28** | **Today's AI Summary Enriched with Leads, Orders, Revenue & Broadcasts** | `tenant-stats.service.ts` | Expanded `todaySummaryEn` and `todaySummaryBn` in `tenant-stats.service.ts` to include Today's real-time metrics for: (1) New leads acquired today, (2) Orders received today and total revenue generated today (BDT), (3) Broadcast campaigns sent today. Kept strictly local (deployment withheld per user request). All 35 unit tests passing. Zero TS errors. |
| **2026-07-28** | **Today's AI Summary Fixed to Today** | `tenant-stats.service.ts`, `dashboard/page.tsx` | Fixed Dashboard AI Summary behavior: (1) Added `todaySummaryEn` and `todaySummaryBn` to `getDashboardOverview()` which compute metrics strictly for TODAY (today's messages, today's AI automation %, today's growth vs yesterday). (2) Updated `dashboard/page.tsx` so "Today's AI Summary" ("আজকের এআই সামারি") banner remains strictly pinned to Today regardless of what date filter range (7d, 15d, 30d, 90d, last month, custom) is selected for the rest of the dashboard. All 23 unit tests passing. Zero TS errors. |
| **2026-07-28** | **WhatsApp Web QR Code & Pairing Flow Interactive Setup** | `whatsapp-web.service.ts`, `inboxes/new/page.tsx` | Fixed WhatsApp Web setup wizard: (1) Added interactive authentication UI with two tabs: **Scan QR Code** and **Pairing Code**. (2) Real-time QR Code rendering using `<QRCode />` connected via Socket.IO namespace `/inbox` for live `whatsapp_qr_code` events. (3) 8-character Pairing Code generator via `POST /whatsapp-web/start-pairing`. (4) Updated `checkWhatsappWebQuota()` in `whatsapp-web.service.ts` to automatically clean up dead/inactive session records, allowing fresh QR code generation without blocking. 17 unit tests passing. Zero TS errors. |
| **2026-07-28** | **Billing History Price Calculation Fix & Table Filters** | `payments.service.ts`, `settings/billing-history/page.tsx` | Fixed 3 billing history issues: (1) Corrected price calculation in `getUpcomingBill()` to prioritize active subscription plan price (e.g. 499 BDT/mo for Starter Plan) instead of multiplying BDT by 120. (2) Always enabled the "Pay / Renew Now" CTA button on the upcoming bill card so users can pay anytime. (3) Added status filter tabs (`All`, `Successful`, `Pending`, `Failed`) and search box for TrxID / package name on the payment history table. 13 unit tests passing. Zero TS errors. |
| **2026-07-28** | **Inboxes Real-Time Status, AI Toggle Fix & Reconnect Button** | `inbox.controller.ts`, `inbox.service.ts`, `inbox.service.spec.ts`, `whatsapp-web.service.ts`, `settings/inboxes/page.tsx` | Fixed 3 inbox issues: (1) Added `isSocketConnected` helper to `whatsapp-web.service.ts` and updated `GET /inbox/channels` to return real-time dynamic connection status (`Active 🟢` vs `Disconnected 🔴`). Added a "Reconnect / Scan QR" CTA button when a channel is disconnected. (2) Added backend route `PATCH /inbox/channels/:id/ai-reply` so the AI Auto-Reply toggle updates `isAiAutoReplyEnabled` cleanly. (3) Ensured `channelConnectionId` caching resolves correctly on incoming messages so message routing to inbox works reliably. 26 unit tests passing. Zero TS errors. |
| **2026-07-28** | **Upcoming Bill Card & Advance Payment** | `payments.service.ts`, `payments.controller.ts`, `payments.service.spec.ts`, `billing-history/page.tsx` | Added `GET /payments/upcoming-bill` endpoint returning current plan name, next renewal date (`currentPeriodEnd`), remaining days, upcoming BDT amount, and `isPaidAdvance` status. Added Upcoming Renewal & Bill Card to `/dashboard/settings/billing-history` with a prominent "Pay Advance / Renew Now" CTA button. Paying in advance extends `currentPeriodEnd` and disables 7d/2d expiration reminder emails. All 19 billing/payment tests passing. Zero TS errors. |
| **2026-07-28** | **Executive BI Dashboard & YouTube Analytics Filters** | `tenant-stats.service.ts`, `tenant-stats.controller.ts`, `tenant-stats.service.spec.ts`, `dashboard/page.tsx` | Built a $100M SaaS-grade Executive Business Intelligence Dashboard. Backend: expanded `tenant-stats.service.ts` to compute comprehensive KPIs, channel health, team performance, CRM pipeline, orders revenue, AI spend/tokens, and a 0-100 `businessHealthScore`. Added YouTube-style date range filters (`today`, `7d`, `15d`, `30d`, `90d`, `this_month`, `last_month`, `custom` dates) to endpoints `GET /stats/tenant/dashboard` and `GET /stats/tenant/charts`. Frontend: complete rewrite of `dashboard/page.tsx` with AI Executive Summary banner, 6 KPI cards, Glassmorphism cards, Recharts Area/Bar/Pie charts, recent conversations & orders tables, operational health widgets, and interactive date range selector. 6 backend unit tests passing, zero TS errors. |
| **2026-07-28** | **Inboxes 3-Issue Fix + Unit Tests** | `whatsapp-web.service.ts`, `whatsapp-web.module.ts`, `whatsapp-web.service.spec.ts`, `billing.service.ts`, `billing.service.spec.ts`, `inboxes/new/page.tsx` | Fix 1: WhatsApp Web messages now reliably reach inbox — `connectionIds` Map caches `tenantId→connectionId` in memory on socket open, preventing the race condition where first incoming messages had `channelConnectionId=undefined`. Fix 2: Duplicate WEB_QR sessions blocked — `checkWhatsappWebQuota()` in `startQr()`/`startPairing()` enforces plan limits AND rejects duplicate WEB_QR connections. Fix 3: Channel selection page now fetches `/billing/quotas` and shows per-channel usage bars (X/Y), locks channels at limit with orange badge, shows upgrade CTA. 15 unit tests passing (7 billing + 8 whatsapp-web). Zero TS errors. |
| **2026-07-28** | **Subscription Page Overhaul** | `settings/subscription/page.tsx` | Complete rewrite. Fixed: unreadable text (bad indentation), switched from `bg-white/70` to Glassmorphism (`bg-surface/70 backdrop-blur-xl`), added complete 13-feature `FEATURE_MAP` (synced with packages/page.tsx & tenants/page.tsx), correct active plan detection via `subscription.planId`, removed old inline payment modal (now redirects to `/billing/pay-mfs`), added storage addon type support, bilingual EN/BN labels, mobile-first responsive grid, promo pricing support. Zero TS errors. |
| **2026-07-28** | **Sync Templates from Meta (WABA Sync)** | `broadcasts.service.ts`, `broadcasts.controller.ts`, `broadcasts/page.tsx` | Added `syncTemplatesFromMeta()` service method that calls Meta Graph API `GET /{wabaId}/message_templates` and upserts all templates into local DB. New templates are created; existing ones get their status/rejection reason updated. Added `POST /broadcasts/templates/sync-from-meta` backend endpoint. Added "Sync from Meta" button (with spinning icon) in the My Templates tab header on the frontend. Sends notification to tenant owner on sync completion. |
| **2026-07-28** | **AI Vision Unit Testing Completion** | `ai.service.spec.ts` | Added comprehensive unit tests for `isVisionSupported` logic across OpenAI, Gemini, and Anthropic. Added tests to verify multi-provider image path processing, base64 conversion, and inline data payload submission inside `generateCompletion`. All tests passing. |
| **2026-07-27** | **AI Product Image Vision Analysis & Catalog Matching** | `orchestrator.service.ts`, `ai.service.ts`, `superadmin/settings/ai/page.tsx`, `ai-training/page.tsx`, `inbox/page.tsx`, `orchestrator.service.spec.ts` | Enabled AI Assistant image processing in Live Inbox. Supports Gemini, Claude & OpenAI vision models to recognize customer product photos and match them with tenant Product Catalog. Enforced 5 AI credits deduction per image analysis (Option A - 5 `aiUsageLog` entries). Included text fallback mode (caption + catalog) when non-vision models are used. Added live UI badges in Superadmin AI Settings, Tenant AI Settings, and Live Inbox bubbles. All 138 backend unit tests passing. |
| **2026-07-27** | **Workspace Rule: Desktop & Mobile Native App Responsiveness** | `.agents/AGENTS.md` | Added mandatory rule requiring all web/desktop design and layout changes to be simultaneously optimized for mobile responsive view in a native app style. |
| **2026-07-27** | **Live Inbox Horizontal 3-Column Layout & Desktop Fix** | `inbox/page.tsx`, `settings/inboxes/page.tsx`, `settings/labels/page.tsx`, `team/page.tsx` | Fixed bug where `Lead Details` displaced chat board due to `flex-col` mode. Set container to `flex flex-col md:flex-row h-full`, placing `Lead Details` on the far right on desktop. Defaulted `showLeadInfo` to `false`. Added missing page imports (`useLanguage`, `toast`, `Save`, `fetchLabels`). Passed Next.js build (`16.4s`) and deployed to live server (`a53e5af`). |
| **2026-07-27** | **Sticky Pinned Mobile Chat Header & Vibrant Gradient Bubbles** | `inbox/page.tsx`, `leads/page.tsx`, `SupportWidget.tsx` | Applied `sticky top-0 z-30 shrink-0` to mobile chat header with locked internal message scrolling (`overflow-y-auto`). Enhanced chat styling with emerald gradient outbound bubbles (`from-[#1F824A] to-[#166538]`), white inbound cards, and soft background wallpaper. Redesigned action toolbars across Leads, Products, and Orders to non-wrapping flex-col layout on mobile. |
| **2026-07-27** | **Mobile Lead Details Back Button & Instruction Banners** | `inbox/page.tsx`, `InstructionBanner.tsx`, `SupportWidget.tsx`, tenant pages | Added `< ChevronLeft /> Back` button in mobile Lead Details header (`md:hidden`). Created reusable `<InstructionBanner />` with collapsible mobile accordion. Elevated floating Support Widget position on mobile (`bottom-16`) to avoid blocking navigation bars. Compacted form inputs (`text-[12px]`). |
| **2026-07-27** | **Lead Details Section Reordering & Solid Color Contrast** | `inbox/page.tsx`, `ClientLayout.tsx` | Redesigned Lead Details header with single-line `whitespace-nowrap` layout. Reordered sections to place `Notes` directly below `Lead Status`. Compacted form fields (`p-2 font-[10px]`). Added solid background contrast (`bg-[#ebedf0]`) and compact plan header badges (`text-[9px]`). |
| **2026-07-27** | **Support UI Overhaul, Label Form & Real-time Web Alerts** | `support/page.tsx`, `LabelForm.tsx`, `inbox/page.tsx`, `tickets.service.ts` | Compacted Support Center UI, fixed light-mode contrast, and expanded reply workspace. Extracted `LabelForm.tsx` with color swatches. Reduced Inbox conversation list width (`w-48/w-52`). Updated `tickets.service.ts` to trigger real-time web notifications (`createNotification`) for tenant users upon Superadmin ticket reply. |
| **2026-07-27** | **Desktop Layout Squeezing Fix & Native Mobile Nav** | `ClientLayout.tsx`, `inbox/page.tsx` | Reduced sidebar width to 165px, conversation list to `w-56/w-60`, and lead details to `w-52/w-56` to prevent layout breaking on 1366px/1440px laptops. Built native mobile bottom navigation bar (`Home`, `Inbox`, `Leads`, `Orders`, `Menu`) and single-pane chat flow for mobile viewports. |
| **2026-07-27** | **Backend Unit Testing & Controller Test Coverage** | `leads.service.spec.ts`, `leads.controller.spec.ts`, `labels.controller.spec.ts`, `inbox.controller.spec.ts` | Completed full backend unit testing for recently added modules. Created test suites for Leads, Labels, and Inbox. Fixed missing mock errors (`broadcastToTenant`) and increased Excel generation timeout. 137/137 backend unit tests passing cleanly. |
| **2026-07-27** | **JwtStrategy Fix & Inbox Viewport Height** | `jwt.strategy.ts`, `inbox/page.tsx` | Fixed bug in `JwtStrategy` where `req.user.id` was missing from JWT payload, causing 500 errors on ticket creation. Corrected Inbox layout height to `h-[calc(100vh-48px)]`. |
| **2026-07-27** | **Bilingual UI Instructions & Form Bug Fixes** | `support/page.tsx`, `leads/page.tsx`, `team/page.tsx`, `labels/page.tsx`, `ai-training/page.tsx` | Added bilingual (EN/BN) instruction headers across settings pages. Replaced `localStorage` with `Cookies.get('access_token')` in Support page. Removed duplicate "Assigned To" column in Leads table. |
| **2026-07-27** | **Global Templates & Library System** | `schema.prisma`, `broadcasts.service.ts`, `broadcasts.controller.ts`, `superadmin/templates/page.tsx`, `broadcasts/page.tsx` | Added `GlobalTemplate` Prisma model and `global_templates` table. Extended `BroadcastsService` with 6 library methods (import, promote, CRUD) and added 7 API routes. Overhauled Superadmin Templates page into 2-tab view (Meta Monitoring + Global Library). Upgraded tenant Broadcasts page to 3-tab layout (Campaigns, My Templates, Template Library) with split-panel import modal and live WhatsApp preview. |
| **2026-07-27** | **Label Creation in Live Inbox & AI Instruction Sync** | `inbox/page.tsx`, `labels/page.tsx`, `LabelForm.tsx`, `labels.controller.ts`, `labels.service.ts` | Extracted `LabelForm.tsx`. Integrated "+ Add" button inside Live Inbox label popover for on-the-fly creation. Added `POST /labels/:id/sync-ai` endpoint and UI button to dynamically inject label instructions into AI System Prompts using `<Label: Name>` tags. |
| **2026-07-27** | **Leads Export to Excel** | `leads.controller.ts`, `leads.service.ts`, `leads/page.tsx` | Added `exceljs` backend dependency. Created `GET /leads/export` endpoint to download `.xlsx` file containing complete lead data (Name, Phone, Email, Stage, Agent, Company, Labels, Notes, Date). Added "Download" button to Leads UI header. |
| **2026-07-27** | **Unified Inboxes Settings (Chatwoot Style) & Meta Auto-Login** | `ClientLayout.tsx`, `inboxes/page.tsx`, `inboxes/new/page.tsx`, Meta SDK buttons | Merged WhatsApp, Messenger, and Instagram setup pages into single `/settings/inboxes` list view. Built multi-step Stepper UI (`inboxes/new`) for dynamic channel connection. Integrated Facebook SDK directly into frontend components for seamless Meta auto-login. Removed deprecated individual channel settings routes. |
| **2026-07-27** | **Inbox Unread Count & AI Training Cleanup** | `schema.prisma`, `inbox.service.ts`, `inbox.gateway.ts`, `ai-training.service.ts` | Added `unreadCount` tracking to Conversation schema with sidebar and conversation list badges. Removed redundant `replyWhenAssigned` logic from Orchestrator and settings page per requirements. |
| **2026-07-27** | **Per-Conversation AI Toggle & Default Assignment UI** | `schema.prisma`, `inbox.service.ts`, `orchestrator.service.ts`, `inbox/page.tsx` | Added `isAiEnabled` to Conversation schema and built `PATCH /toggle-ai` API. Updated Orchestrator to skip replies when disabled. Added AI Toggle switch to Inbox chat header. Replaced "Unassigned" labels with "🤖 AI Assistant (Default)". |
| **2026-07-27** | **Quota System Overhaul & Cycle Realignment** | `quota.service.ts`, `billing.service.ts`, `broadcasts.service.ts`, `payments.service.ts` | Overhauled quota system to enforce strict billing cycle boundaries (`currentPeriodStart` to `currentPeriodEnd`). Replaced accumulative DB fields with real-time queries summing direct messages and non-failed Broadcast Recipients. Added pre-flight quota checks for Bulk Broadcasts. Updated dashboards and rewrote unit tests (25/25 passing). |
| **2026-07-27** | **Contact & Conversation Hard Deletion API & UI** | `inbox.service.ts`, `leads.service.ts`, `inbox/page.tsx`, `leads/page.tsx` | Implemented hard deletion for contacts and conversations with manual cascading (`message.deleteMany`, `order.updateMany`) to respect Windows Prisma lock rules and prevent FK 500 errors. Added deletion UI buttons to Inbox and Leads views. Reordered Settings menu to bottom in Tenant layout. |
| **2026-07-26** | **Android 14 App Crash & UI Cleanup** | `AndroidManifest.xml`, `MainActivity.kt`, `SmsGatewayService.kt`, `mfs/page.tsx` | Fixed Android 14 instant app crash by adding `FOREGROUND_SERVICE_DATA_SYNC` permission and enforcing `Theme.AppCompat`. Cleaned up frontend MFS settings page and deployed compiled `zinichat-sms-gateway.apk` direct download link. |
| **2026-07-26** | **Android SMS Gateway Persistent Service Upgrade** | `SmsReceiver.kt`, `SmsGatewayService.kt`, `BootReceiver.kt`, `MainActivity.kt` | Converted `SmsGatewayService` into a persistent Foreground Service (`START_STICKY`, `onTaskRemoved` AlarmManager restart, `BootReceiver` for phone reboot). Added `goAsync()` thread handling in `SmsReceiver`. Added `RECEIVE_BOOT_COMPLETED`, `POST_NOTIFICATIONS`, and `WAKE_LOCK` permissions. |
| **2026-07-25** | **Contact Normalization & Lead Details Side-by-Side** | `inbox.service.ts`, `inbox/page.tsx`, `SupportWidget.tsx`, `dashboard/page.tsx` | Normalized contact lookup (+ prefix / @c.us suffix) in `inbox.service.ts` to prevent duplicate contact creation on media messages. Converted Lead Details panel to side-by-side flex layout. Compacted floating `SupportWidget` on `/inbox` route. Added `isSetupPending` check to hide setup banner post-onboarding. |
| **2026-07-25** | **Superadmin AI Model Default & Responsive Customize Modal** | `tenants.service.ts`, `superadmin/tenants/page.tsx` | Removed "Platform Default" option from Tenants table AI Model select, auto-assigning active default AI Model ID (`isActive: true`). Made Tenant "Customize Plan" modal scrollable internally (`max-h-[90vh] overflow-y-auto`). |
| **2026-07-25** | **Active Plan Resolution, Billing & Notification Isolation** | `auth.service.ts`, `payments.service.ts`, `notifications.service.ts` | Resolved active subscription plan in `getMe()` to fix topbar plan badge showing "FREE" for active users. Auto-cancelled redundant pending checkout attempts in Billing History. Isolated Superadmin vs Tenant notifications to filter system alerts appropriately. |
| **2026-07-25** | **WhatsApp Meta Template System & Live Phone Preview** | `schema.prisma`, `broadcasts.service.ts`, Meta APIs, `broadcasts/page.tsx` | Integrated Meta Graph API (`POST /message_templates`) and Resumable Upload API for Media Headers (Image/PDF/Video). Implemented Webhook status listener for APPROVED/REJECTED updates with real-time Socket alerts. Created tenant Template Builder with variable inputs and live phone preview. |
| **2026-07-25** | **Superadmin Storage & Templates Dashboard** | `packages/page.tsx`, `storage.service.ts`, `superadmin/templates/page.tsx` | Added plan-wise and custom tenant `storageLimitMb` controls with 1-click `POST /storage/clear-all` cleanup button. Created Superadmin Message Templates Approval dashboard. Fixed MFS unit tests (90/90 passing). |
| **2026-07-25** | **Tenant UI Glassmorphism & Support 401 Fix** | `ClientLayout.tsx`, `support/page.tsx` | Enforced Unified Glassmorphism Single Mode by stripping `dark:` classes across tenant dashboard. Overhauled sidebar to use primary green (`#1F824A`) and secondary orange (`#EE8D27`). Fixed support ticket creation by switching from localStorage to Cookies.get('access_token'). |
| **2026-07-25** | **Tenant Active Plan & Custom Plan Sync** | `tenants.service.ts`, `superadmin/tenants/page.tsx`, `AGENTS.md` | Updated `tenantsService.findAll()` to filter active subscriptions (`status === 'active'`/`trialing'`), ignoring unpaid pending checkouts. Synchronized Customize Plan modal fields with Packages page (Seat, WA, Messenger, Instagram limits + `customAllowByok`). Added Rule 23 to AGENTS.md. |
| **2026-07-25** | **Support UI & AI Escalation Notifications** | `support/page.tsx`, `support-chat.service.ts` | Added `react-hot-toast` notifications for support ticket creation. Updated `SupportChatService` to inject `NotificationsService` and `SmtpService`, sending real-time web alerts and emails to Superadmins upon AI escalation. |
| **2026-07-25** | **Per-Channel AI Auto-Reply Toggle & Platform Channel Limits** | `schema.prisma`, `whatsapp-auth.controller.ts`, `packages/page.tsx` | Added boolean `isAiAutoReplyEnabled` to `ChannelConnection` model. Allowed tenants to toggle AI auto-replies per channel connection. Split unified `channelLimit` into `whatsappLimit`, `messengerLimit`, and `instagramLimit` across DB, Quota Service, and UI. |
| **2026-07-25** | **Dynamic Feature Gating & Locked Menus UI** | `ClientLayout.tsx` | Implemented UI to show locked sidebar menus with a lock icon when features are disallowed by tenant plan. Added Feature Locked upgrade modal and prevented direct URL access. |
| **2026-07-24** | **Epay MFS Gateway Portal & Checkout Optimization** | `pay-mfs/page.tsx`, `payments.service.ts`, `mfs-payments.service.ts`, `CurrencyProvider.tsx` | Built dedicated checkout portal matching Epay/Paystation UI (`/dashboard/billing/pay-mfs`) with category tabs, active account filtering, real-time timer, and auto-redirect. Replaced fraction offset calculations and manual QR generation with direct BDT formatting (`formatBdtDirect`) and Superadmin uploaded QR image priority (`qrCodeUrl`). Fixed currency double-conversion bug in `CurrencyProvider.tsx`. Created Tenant Billing History page with retry actions and backend auto-cancellation. Fixed Android APK GitHub Actions workflow (`build-android-apk.yml`, `assembleDebug`). |
| **2026-07-24** | **Zero-Config ZiniChat Android Gateway App & APK Hosting** | `android-sms-gateway/`, `settings/mfs/page.tsx`, `public/downloads/` | Upgraded custom ZiniChat Android app with built-in zero-config parsers for bKash, Nagad, Rocket, and BD Banks. Hosted compiled privacy-preserving APK directly at `/downloads/zinichat-sms-gateway.apk`. |
| **2026-07-23** | **Multi-Provider AI (Gemini/Claude/OpenAI) & Email Standardization** | `support-chat.service.ts`, `smtp.service.ts`, `facebook-auth/page.tsx` | Resolved Gemini base URL (`https://generativelanguage.googleapis.com/v1beta/openai/`) and model fallbacks in `SupportChatService`. Enforced role session isolation in `middleware.ts`. Standardized official contact info (`01533894967`, `#386, Uttar Badda, Dhaka-1212`) and email routing (`info@zinichat.com` for contact, `support@zinichat.com` for alerts). Refactored `SmtpService` templates and unit tests (89/89 tests passing). Overhauled Superadmin Facebook Auth setup UI with multi-channel OAuth redirect URIs. |
| **2026-07-22** | **BullMQ Sequential Broadcast Processor & Statistical Reports** | `broadcasts.processor.ts`, `tenants.service.ts`, `stats.service.ts` | Built `BroadcastsProcessor` with BullMQ sequential recipient queueing and delays to eliminate system crashes and Meta API rate-limiting during multi-tenant campaigns. Enhanced Superadmin Tenants list with subscription status tracking and deep statistical reports. Added Business Nature pie chart visualization on Superadmin Dashboard. |
| **2026-07-21** | **Feature Gating, Promotional Pricing & HNSW Vector Indexing** | `schema.prisma`, `PricingSection.tsx`, `site-editor/page.tsx` | Implemented strict feature gating for Support AI, Team, and Labels with locked menu overlays and upgrade modals. Added promotional pricing schema (`promoPriceMonthlyBdt`, `promoMonths`, `yearlyDiscountPercent`) with automatic discount calculation. Created dynamic legal pages (Privacy, Terms, Contact, Socials) managed via Site Editor. Implemented HNSW index on `knowledge_chunks` vector column for fast vector similarity search. Conducted load testing on VPS (1,500+ official / 50+ Baileys tenants per 8GB RAM node). |
| **2026-07-20** | **Traefik Deployment Infrastructure & Notification System Fixes** | `.env.deploy`, `leads.cron.ts`, `tickets.service.ts`, `payments.service.ts` | Deployed live (`zinichat.com`) and staging (`test.zinichat.com`) environments behind Traefik proxy with automatic SSL and isolated Docker network routing. Updated background services (`LeadsCronService`, `TicketsService`, `PaymentsService`) to target both `owner` and `admin` roles, ensuring Google OAuth tenants receive real-time web alerts. |
| **2026-07-19** | **Local MCP Deployment & Server Health Monitoring** | `scripts/mcp-deploy-server.js`, `mcp-config.json` | Built local MCP server using `node-ssh` to execute secure server deployments (`deploy_test_server`, `deploy_live_server`) and health monitoring (`check_server_health`, `get_docker_logs`, `restart_services`) via SSH Key auth without external CI pipelines. |
| **2026-07-15** | **Security Hardening, Support Ticketing & Public Inquiries CRM** | `main.ts`, `TicketsModule`, `InquiriesModule`, `smtp.service.ts` | Implemented strict CORS, Helmet, Throttler rate limiting, ValidationPipe, FileInterceptor filtering, and secure SameSite JWT cookies. Built end-to-end Support Ticketing system and Public Contact Inquiries CRM with dynamic SMTP/Web notification hooks. Built 3-Tier BDT pricing engine with Superadmin coupon management. Implemented `QuotaService` for messaging, storage, and AI usage. |
| **2026-07-15** | **Unified Glassmorphism UI, CRM Pipeline & Setup Journey** | `globals.css`, `leads.service.ts`, `orchestrator.service.ts`, `SetupJourneyWidget.tsx` | Redesigned Tenant Panel into single unified Glassmorphism theme with brand colors (`#1F824A` Green & `#EE8D27` Orange). Added "Intake" default stage to Leads CRM Kanban with full CRUD UI. Built AI master switch (`isActive`, `replyWhenAssigned`). Built `SetupJourneyWidget` post-signup onboarding widget. |
| **2026-07-14** | **WhatsApp Media Attachments & Multi-Channel Routing** | `whatsapp-web.service.ts`, `whatsapp.processor.ts`, `inbox.service.ts` | Implemented Baileys high-res image/video media download to `/uploads` directory with base64 thumbnails and full zoom preview in Inbox UI. Built `POST /inbox/messages/media` for outbound media sending via BullMQ `whatsappQueue`. Added `channelConnectionId` to `Conversation` schema for strict multi-line message routing. |
| **2026-07-13** | **WhatsApp Web (Baileys) Integration & Anti-Ban Rate Limiting** | `whatsapp-web.service.ts`, `whatsapp.processor.ts`, `whatsapp/page.tsx` | Built `WhatsappWebService` with Baileys using zero-caching, pairing code authentication, and anti-ban rate limiting (10 msg/min per tenant). Built WhatsApp Website Widget generator (`WidgetController`). Fixed notification service signature. |
| **2026-07-11** | **Core Architecture, Real-Time Socket Gateway & Security Audit** | `schema.prisma`, `inbox.gateway.ts`, `auth.controller.ts`, `permissions.guard.ts` | Built NestJS backend + Next.js frontend foundation with PostgreSQL (`pgvector`), Redis/BullMQ, Socket.io, and Prisma. Implemented RBAC employee permissions, Google & Facebook OAuth integration, real-time web notifications (`NotificationsModule`), dynamic currency exchange engine (`CurrencyProvider`), SMTP email templates (`SmtpModule`), and security hardening (CORS allowlist, ValidationPipe, JWT cookie checks). |

---

## 5. Directory Structure & Key Files
* `full-platform-architecture-v3.md`: Primary architectural specification.
* `project-context.md`: Living developer context log (this file).
* `.agents/AGENTS.md`: Workspace rules enforcing mandatory context tracking, Git safety, and UI guidelines.
* `backend/`: NestJS modular monolith backend application (`src/inbox`, `src/broadcasts`, `src/quota`, `src/whatsapp`, `src/auth`, `src/labels`, etc.).
* `frontend/`: Next.js 16 app router application containing Tenant Dashboard (`app/(tenant)/dashboard/`) and Superadmin Portal (`app/superadmin/`).
* `android-sms-gateway/`: Proprietary Kotlin Android app for zero-config MFS & Bank SMS payment matching.
* `scripts/`: MCP Deployment & Monitoring server (`mcp-deploy-server.js`) and invocation helper (`invoke-mcp.js`).
* `.env.live` / `.env.test`: Deployment configurations for production and test Docker Compose environments.

---

## 6. Production Deployment & Architecture Summary
* **Traefik Reverse Proxy**: Automatically routes HTTPS traffic to frontend (`:8200` live / `:8100` test) and backend (`:8201` live / `:8101` test) containers with SSL certificates.
* **Database & Queues**: PostgreSQL with `pgvector` (HNSW indexing) for AI RAG embeddings, Redis + BullMQ for asynchronous outbound messaging and broadcast job processing.
* **Storage**: Persistent Docker volume (`zinichat_backend_uploads`) mapped to `./uploads` for user avatars, media attachments, and QR code assets.
* **Deployment Protocol**: Local PC pushes code to GitHub (`git push origin main`), then invokes `scripts/invoke-mcp.js` to execute automated remote server pulls (`git pull`) and container recreation (`docker compose up -d`). Direct file transfers or unapproved `git push` actions are strictly prohibited.
